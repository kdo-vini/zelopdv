{
  "name": "zelopdv-landing-video",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Videos de produto (Remotion) para a landing do ZeloPDV. Isolado do app principal (SvelteKit) - nao entra no build/check/vitest raiz.",
  "scripts": {
    "studio": "remotion studio",
    "still": "remotion still",
    "render": "remotion render",
    "probe": "node scripts/probe.mjs",
    "frames": "node scripts/extract-frames.mjs",
    "build-all": "node scripts/render-all.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "remotion": "4.0.528",
    "@remotion/cli": "4.0.528"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "ffmpeg-static@5.3.0": true
  }
}
