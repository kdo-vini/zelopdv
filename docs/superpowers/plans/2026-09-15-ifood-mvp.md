# Integração iFood MVP — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir que uma empresa conecte uma loja iFood, opere todos os pedidos normais no ZeloPDV e materialize, ao receber `CONCLUDED`, uma venda bruta idempotente fora do caixa, visível nos relatórios e compreendida pelo Zelinho.

**Architecture:** Um endpoint SvelteKit valida e persiste webhooks; um worker Node sempre ativo no Dokploy processa inbox, polling, comandos e presença por merchant. O módulo profundo `IfoodIntegration` traduz o protocolo externo para `zelo_orders`; Supabase mantém inbox/outbox, leases e identidades idempotentes; Pedidos, Cozinha, relatórios e Zelinho consomem apenas contratos internos.

**Tech Stack:** SvelteKit 2, Svelte 5, Node.js 24, Supabase/PostgreSQL, Vitest, Playwright e Docker/Dokploy.

**Spec:** `docs/superpowers/specs/2026-09-15-ifood-mvp-design.md`

**ADR:** `docs/adr/0001-ifood-worker-dedicado.md`

## Ordem linear obrigatória

Nenhuma task pode ser iniciada antes de a anterior estar concluída. Não existem trilhas paralelas neste plano. **Cada task produz exatamente um commit.** A ordem é:

| Ordem | Task | Resultado | Status | Commit obrigatório |
|---:|---|---|---|---|
| 1 | Congelar o contrato externo | Coleção/rotas/eventos reais e fixtures sanitizadas | Concluída | `docs: freeze iFood Order and Events contract` |
| 2 | Criar o núcleo de domínio | Contratos, políticas e adapter mock | Concluída | `feat: add iFood integration domain core` |
| 3 | Criar a fundação no banco | Schema privado, identidades e RPCs de lease | Concluída — harness local verificado; claims sequenciais não provam concorrência | `feat: add iFood integration persistence` |
| 4 | Criar o processo worker | Runtime, healthcheck, Docker e shutdown | Concluída | `feat: add dedicated iFood worker runtime` |
| 5 | Implementar o adapter HTTP | Token, rate limit, Order, Events e Merchant | Concluída | `feat: add production iFood HTTP adapter` |
| 6 | Receber webhook com durabilidade | HMAC sobre bytes crus e resposta 202 pós-commit | Concluída | `feat: persist signed iFood webhooks` |
| 7 | Processar a inbox | Lease, retry, dead-letter e reentrada | Concluída | `feat: process durable iFood event inbox` |
| 8 | Projetar pedidos canônicos | Detalhe, normalização e `zelo_orders` | Concluída — validada só no harness local | `feat: project iFood orders into canonical queue` |
| 9 | Reconciliar polling e presença | Poll, ACK, health e fail-closed | Concluída | `feat: reconcile iFood events and presence` |
| 10 | Enviar comandos assíncronos | Ações, motivos e confirmação por evento | Concluída | `feat: add asynchronous iFood order commands` |
| 11 | Integrar Pedidos e Cozinha | Badge, SLA, agendados e estado de sync | Concluída | `feat: show iFood orders in operational queues` |
| 12 | Mapear produtos e estoque | Vínculo explícito, compromisso e compensação | Concluída | `feat: add progressive iFood product mapping` |
| 13 | Definir o dono da impressão | Zelo ou externo, nunca ambos | Concluída | `feat: coordinate iFood order printing` |
| 14 | Materializar venda e estorno | Venda em `CONCLUDED`, sem caixa, e estorno auditável | Concluída no harness descartável local; migration não aplicada em produção | `feat: materialize iFood sales and reversals` |
| 15 | Expor canal nos relatórios | Filtro, cards, origem, cancelamentos e estornos | Concluída | `feat: report sales by origin channel` |
| 16 | Dar contexto de canal ao Zelinho | Resumos gerais e comparações sem PII | Concluída | `feat: teach Zelinho sales channel context` |
| 17 | Criar APIs self-service | Autorização, owner, entitlement e health | Concluída | `feat: add self-service iFood connection APIs` |
| 18 | Criar o wizard de conexão | Fluxo progressivo em Perfil > Integrações | Concluída | `feat: add self-service iFood setup wizard` |
| 19 | Criar suporte operacional | Saúde, kill switches, replay e auditoria | Concluída (migration local; não aplicada em prod) | `feat: add iFood operations console` |
| 20 | Fechar qualidade e deploy | E2E, falhas, CI e imagem do worker | Concluída (Docker skip local — CLI ausente) | `test: qualify iFood MVP for pilot` |
| 21 | Executar piloto e fechar rollout | Evidências, soak e decisão de liberação | Concluída como **NO-GO** documentado (sem mutação prod) | `docs: record iFood MVP pilot decision` |

## Divisão de execução (decisão do dono, 2026-09-16)

A partir da Task 11, cada task é dividida por camada:

- **Backend → Codex CLI** (`gpt-5.6-luna`, effort `max`): migrations e RPCs,
  rotas `src/routes/api/**`, módulos `src/lib/server/**`, worker
  `workers/ifood/**` e seus testes. O Codex não commita, não aplica migration e
  não roda o harness; o arquivo de migration é gerado antes pelo coordenador com
  o Supabase CLI oficial (a sandbox do Codex não consegue gravar a telemetria do
  CLI).
- **Frontend → Claude** (coordenador): telas `src/routes/**/+page.svelte`,
  componentes `src/lib/components/**`, apresentação/cliente do browser em
  `src/lib/**` fora de `server/`, e seus testes — sempre lendo
  `docs/DESIGN_PATTERNS.md` antes.
- **Coordenação → Claude:** define o contrato entre as camadas antes de
  despachar, revisa o backend linha por linha, roda testes e harness, aplica
  migrations autorizadas e faz um único commit por task.

Tasks puramente de backend (ou puramente de frontend) vão inteiras para a
camada correspondente.

## Contrato de documento vivo

Este arquivo é o tracker canônico da implementação e **deve ser alterado em todo commit das Tasks 1–21**.

Antes de concluir cada task, o executor deve:

1. marcar todos os seus checkboxes;
2. trocar o status da linha correspondente para `Concluída` ou `Bloqueada`;
3. preencher o bloco `Resultado real` da task com data, comandos executados, contagens e desvios;
4. atualizar `docs/CURRENT.md` com o estado verificável da integração;
5. incluir este plano e `docs/CURRENT.md` no mesmo commit da implementação.

O SHA do próprio commit não é gravado dentro dele, pois isso criaria uma referência recursiva que muda a cada amend. O assunto de commit da tabela é a identidade estável. O SHA é resolvido com:

```powershell
git log --fixed-strings --grep "<assunto exato>" --format="%H %s"
```

Uma task nunca é considerada concluída apenas porque o código foi escrito. O teste RED deve falhar pelo motivo esperado; o teste GREEN deve passar; o diff deve estar limpo de placeholders e `git diff --check` deve passar. Se houver falha conhecida, a task fica `Bloqueada` e o commit registra somente evidência recuperável — nunca código parcialmente habilitado.

## Restrições globais

- Usar TDD: RED, implementação mínima, GREEN, refatoração.
- Gerar toda migration com `supabase migration new <nome>`; nunca inventar ou renomear timestamp manualmente.
- Não aplicar migration no projeto vinculado nem publicar imagem/branch sem autorização explícita do dono.
- Tabelas de integração ficam em `ifood_internal`, sem grants para `anon` ou `authenticated`.
- RPCs públicas que atravessam o schema privado são `SECURITY DEFINER`, fixam `search_path`, detectam service role pelo contrato vigente, revogam `PUBLIC` e concedem somente o mínimo.
- Browser nunca recebe `clientSecret`, access token, payload bruto ou credencial privilegiada.
- Logs e testes não contêm telefone, endereço, CPF, Authorization ou segredo real.
- `event_id`, pedido externo, comando, compromisso de estoque, impressão, venda e estorno têm chaves idempotentes independentes.
- Um HTTP `202` de comando iFood não muda o estado comercial; apenas evento externo confirmado muda `zelo_orders.status`.
- Toda venda iFood usa `id_caixa = null`, inclusive quando a forma declarada é dinheiro.
- Item não mapeado não bloqueia o pedido e não movimenta estoque.
- O cardápio, Financial, repasses, clientes/Pessoas, aceite automático e backfill histórico permanecem fora do MVP.
- A badge inicial é textual. O logotipo oficial só entra após autorização formal documentada.
- O alerta sonoro genérico do ZeloPDV é outro plano; aqui ele aparece somente como gate de rollout.
- Ao tocar superfícies críticas, reler os documentos exigidos por `AGENTS.md` antes da edição.

---

## Task 1: Congelar o contrato externo liberado ao app

**Bloqueia toda a implementação:** esta task exige conta profissional, app de teste, merchant de teste e a coleção/OpenAPI efetivamente liberada pelo iFood. Se qualquer um faltar, registrar o bloqueio; não adivinhar rotas.

**Files:**
- Create: `docs/integrations/ifood/CONTRACT_SNAPSHOT.md`
- Create: `tests/fixtures/ifood/events/placed.json`
- Create: `tests/fixtures/ifood/events/concluded.json`
- Create: `tests/fixtures/ifood/events/cancelled.json`
- Create: `tests/fixtures/ifood/orders/immediate-ifood-delivery.json`
- Create: `tests/fixtures/ifood/orders/immediate-merchant-delivery.json`
- Create: `tests/fixtures/ifood/orders/immediate-pickup.json`
- Create: `tests/fixtures/ifood/orders/scheduled.json`
- Create: `tests/fixtures/ifood/errors/rate-limit.json`
- Create: `tests/ifood.contract-fixtures.test.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** versão de autenticação, rotas exatas de token/poll/ACK/detalhe/comandos/merchant, escopos, eventos aceitos, headers, limites e polling homologável.

- [x] **Step 1: Exportar a coleção oficial e registrar decisões**

Documentar centralizada versus distribuída, onboarding de merchant, webhook, presença granular e intervalo de fallback. O snapshot deve citar a data e a URL oficial de cada contrato, mas não copiar tokens ou IDs reais.

- [x] **Step 2: Criar fixtures mínimas sanitizadas**

Preservar formato, tipos e códigos; substituir identificadores e PII por valores como `merchant-fixture`, `order-fixture` e `Cliente Teste`.

- [x] **Step 3: Escrever o teste de contrato**

```js
expect(order.category).toBe('FOOD');
expect(order.merchant.id).toBeTypeOf('string');
expect(new Set(['IMMEDIATE', 'SCHEDULED'])).toContain(order.orderTiming);
expect(new Set(['DELIVERY', 'TAKEOUT'])).toContain(order.orderType);
expect(event.id).toBeTypeOf('string');
```

O teste também garante que nenhuma fixture contém padrões de Bearer token, CPF, telefone brasileiro real ou endereço real.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/ifood.contract-fixtures.test.js`

Expected: PASS para todas as modalidades do MVP.

- [x] **Step 5: Atualizar o tracker e commitar**

```powershell
git add docs/integrations/ifood tests/fixtures/ifood tests/ifood.contract-fixtures.test.js docs/superpowers/plans/2026-09-15-ifood-mvp.md docs/CURRENT.md
git commit -m "docs: freeze iFood Order and Events contract"
```

**Resultado real (2026-09-15):**

- **Status: Concluída.** A coleção Events v1 completa fornecida nesta sessão
  confirmou a base, headers, filtros, envelopes, ACK, `202` e limite de 10.000;
  a coleção bruta não foi adicionada ao Git.
- Snapshot criado em `docs/integrations/ifood/CONTRACT_SNAPSHOT.md`, com URLs
  oficiais, evidência real sanitizada e separação explícita entre observado e
  sintético/não homologado.
- Fixtures criadas para `PLACED`, `CONCLUDED`, `CANCELLED`, quatro modalidades
  de pedido e `429`. A forma real comprovada foi app centralizado,
  `client_credentials`, Merchant list/status `200`, Events polling `204` vazio,
  dois eventos `PLACED` e Order detail `200`; `/order/v1.0/orders:polling`
  retornou `404`. A conta gerou dois pedidos automáticos durante a sessão;
  o polling posterior retornou seis reentregas at-least-once, o ACK em lista de
  IDs respondeu `202` e o polling seguinte respondeu `204`. Nenhum pedido
  adicional foi gerado deliberadamente para esta entrega; IDs/PII não foram
  preservados.
- RED: `npx vitest run tests/ifood.contract-fixtures.test.js --reporter=verbose`
  falhou como esperado por 8 fixtures ainda ausentes (13 testes falhando e o
  teste de placeholders passando).
- GREEN: o mesmo comando passou com 1 arquivo e 14 testes aprovados.
- `git diff --check` passou. Webhook, comandos, presença granular, resposta
  `429`/limites, modalidades não observadas e homologação continuam lacunas
  documentadas; o ACK Events v1 está comprovado. A Task 2 está liberada para
  iniciar, sem integração habilitada em runtime.

## Task 2: Criar o núcleo puro do módulo `IfoodIntegration`

**Files:**
- Create: `src/lib/server/ifood/contracts.js`
- Create: `src/lib/server/ifood/eventPolicy.js`
- Create: `src/lib/server/ifood/orderNormalizer.js`
- Create: `src/lib/server/ifood/createIfoodIntegration.js`
- Create: `src/lib/server/ifood/adapters/mockIfoodAdapter.js`
- Create: `tests/ifood.domain.test.js`
- Create: `tests/ifood.order-normalizer.test.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `createIfoodIntegration(deps)`, `normalizeIfoodOrder(order)`, `decideEventTransition(current,event)` e adapter mock determinístico.

- [x] **Step 1: Escrever testes RED para a interface profunda**

Cobrir duplicidade, inversão, evento desconhecido, imediato/agendado, três modalidades, complementos, descontos, pagamento e remoção de PII da projeção analítica.

```js
const integration = createIfoodIntegration({ adapter, repository, clock });
await integration.receiveEvent(placedEvent);
expect(repository.appendEvent).toHaveBeenCalledOnce();
```

- [x] **Step 2: Rodar RED**

Run: `npx vitest run tests/ifood.domain.test.js tests/ifood.order-normalizer.test.js`

Expected: FAIL por módulos inexistentes.

- [x] **Step 3: Implementar contratos sem I/O**

O normalizador retorna somente o contrato interno:

```js
{
  externalOrderId, merchantId, externalStatus, occurredAt,
  customerSnapshot, fulfillment, payment, totals, items,
  scheduled, preparationStartAt, pickupCode, deliveryCode
}
```

`eventPolicy` mantém rank monotônico e exceções explícitas para `CANCELLED` e `CONCLUDED`; código desconhecido resulta em `quarantine`, nunca em regressão silenciosa.

- [x] **Step 4: Rodar GREEN e suíte vizinha**

Run: `npx vitest run tests/ifood.domain.test.js tests/ifood.order-normalizer.test.js tests/onlineOrders.test.js`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add iFood integration domain core`

**Resultado real (2026-09-15):**

- **Status: Concluída.** O módulo profundo concentra a política de transição e
  a orquestração de recebimento atrás de cinco operações (`connectMerchant`,
  `receiveEvent`, `requestOrderAction`, `reconcileEvents` e
  `getConnectionHealth`); adapter e repository entram por dependências
  injetadas. Não há I/O real, rede, credenciais ou side effects externos.
- RED: `npx vitest run tests/ifood.domain.test.js tests/ifood.order-normalizer.test.js --reporter=verbose`
  falhou como esperado em 2 suítes por módulos ausentes (0 testes coletados).
- GREEN focado: o mesmo comando passou com 2 arquivos e 14 testes aprovados.
- GREEN com vizinha: `npx vitest run tests/ifood.domain.test.js tests/ifood.order-normalizer.test.js tests/onlineOrders.test.js --reporter=verbose`
  passou com 3 arquivos e 23 testes aprovados.
- Suíte integral: `npm test` passou com 202 arquivos, 1.240 testes aprovados e
  3 skips condicionais. `npm run check` passou com 0 erros e 0 warnings.
- A cobertura inclui deduplicação, inversão/ranking monotônico, exceções
  explícitas `CANCELLED`/`CONCLUDED`, desconhecido em `quarantine`, pedidos
  imediatos/agendados, entrega iFood/merchant e retirada, complementos,
  descontos, pagamento e snapshot operacional de comprador sem campo
  analítico/PII duplicado. O mock é determinístico e só mantém estado em
  memória para testes.
- Desvio recuperável: a tentativa inicial `npm test -- --runInBand` foi
  rejeitada pelo Vitest 3.2.7 por opção inexistente; `npm test` foi executado
  em seguida e passou. `git diff --check` passou antes do commit.

**Revisão corretiva de qualidade (2026-09-15, mesmo commit via amend):**

- RED específico: `npx vitest run tests/ifood.domain.test.js tests/ifood.order-normalizer.test.js --reporter=verbose`
  falhou com 14 testes e 13 aprovados (27 listados), cobrindo os achados de
  terminal ambíguo, números/enums/totais, pagamentos split, quarantine durável
  e aliases mutáveis.
- GREEN focado + vizinha: `npx vitest run tests/ifood.domain.test.js tests/ifood.order-normalizer.test.js tests/onlineOrders.test.js --reporter=verbose`
  passou com 3 arquivos e 36/36 testes aprovados.
- A política agora exige timestamps confiáveis e estritamente posteriores para
  substituir `CANCELLED`/`CONCLUDED`; conflito igual, inválido, ausente ou
  antigo vai para `quarantine`. O normalizador falha fechado para dinheiro,
  quantidade, enums e invariantes de totais inválidos; mapeia pagamentos iFood
  para IDs canônicos, preserva splits e mantém desconhecidos como IDs externos
  sanitizados. A integração exige `appendEvent` e `quarantineEvent` duráveis,
  sem cache ilimitado de IDs; falha de quarantine pode ser reexecutada. Itens
  expõem apenas `options`, sem aliases com referências mutáveis.

## Task 3: Criar persistência privada, identidades e leases

**Files:**
- Create via CLI: `supabase/migrations/*_ifood_mvp_foundation.sql`
- Create: `supabase/verification/ifood_mvp_foundation.sql`
- Create: `tests/ifood.persistence-schema.test.js`
- Modify: `docs/data/SCHEMA_RLS.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `ifood_internal.connections`, `event_inbox`, `order_refs`, `order_commands`, `product_mappings`, `stock_commitments`; RPCs service-role-only de enqueue/claim/finish; `zelo_orders.source='ifood'`.

- [x] **Step 1: Gerar a migration pela CLI**

Run: `supabase migration new ifood_mvp_foundation`

Registrar no `Resultado real` o caminho criado pela CLI.

- [x] **Step 2: Escrever o teste RED de schema**

Exigir schema privado, uniques externas, leases com `FOR UPDATE SKIP LOCKED`, tamanho/retenção de payload, grants mínimos e ausência de grants de browser.

- [x] **Step 3: Implementar a migration**

Identidades mínimas:

```sql
unique (merchant_id),
unique (event_id),
unique (merchant_id, external_order_id),
unique (zelo_order_id),
unique (connection_id, external_order_id, intent, expected_external_revision)
```

Criar RPCs `enqueue_ifood_event_v1`, `claim_ifood_events_v1`, `finish_ifood_event_v1`, `claim_ifood_commands_v1` e `finish_ifood_command_v1`. Todas recebem somente dados necessários, truncam erros e nunca retornam payload bruto ao browser.

- [x] **Step 4: Criar verificação transacional**

Exercitar claims sequenciais, evento duplicado, isolamento de duas empresas,
lease expirado, dead-letter terminal, command key e grants. A verificação roda
dentro de `BEGIN`/`ROLLBACK`; claims sequenciais não provam concorrência entre
workers, que exige duas sessões independentes.

- [x] **Step 5: Validar**

Run: `npx vitest run tests/ifood.persistence-schema.test.js`

Run: `npm run verify:migrations`

Run: `powershell -ExecutionPolicy Bypass -File scripts/verify-supabase-baseline.ps1 -ApplyForwardMigrations -ExcludeTenantDataSeeds -PostMigrationVerification supabase/verification/ifood_mvp_foundation.sql`

- [x] **Step 6: Atualizar tracker e commitar**

Commit: `feat: add iFood integration persistence`

**Resultado real (2026-09-16, reparo do replay):**

- **Status: Concluída no harness descartável.** O bloqueio histórico era uma
  dependência ausente no stream local: o baseline
  `20260813091000` não tinha `public.zelomenu_modifier_components` nem
  `zelomenu_modifier_option_products.id_componente`, mas
  `20260911120000_zelomenu_canonical_pause.sql` já os referenciava. A bridge
  forward-only `20260911110000_zelomenu_canonical_modifier_components.sql`
  recompõe esse contrato antes da migration canônica, é idempotente quando o
  contrato já existe, preserva preços/destinos legados, completa links vazios
  de rollout parcial e valida o CHECK de exatamente um destino. A migration
  canônica histórica não foi alterada.
- A migration iFood foi criada pelo comando obrigatório `supabase migration
  new ifood_mvp_foundation`, sem timestamp/nome inventado, em
  `supabase/migrations/20260916023512_ifood_mvp_foundation.sql`. A CLI local é
  `2.109.1`; `supabase --help`, `supabase migration --help`, `supabase
  migration new --help` e `supabase db advisors --help` foram consultados.
- RED estrito: `npx vitest run tests/ifood.persistence-schema.test.js
  --reporter=verbose` falhou nos 5 testes quando a migration ainda tinha 0
  bytes. GREEN: a mesma suíte passou 5/5 após o SQL.
- A migration cria o schema privado `ifood_internal`, as seis tabelas com RLS,
  identidades únicas, índices de FK/fila, limites de payload/erro/retenção,
  leases com `FOR UPDATE SKIP LOCKED`, CAS de finish, dead-letter, as cinco
  RPCs service-role-only e `zelo_orders.source='ifood'`. O SQL de verificação
  transacional em `supabase/verification/ifood_mvp_foundation.sql` cobre
  duplicata, claims sequenciais de dois tenants, lease expirado, dead-letter
  terminal, command key e ACL; todas as fixtures terminam em `ROLLBACK`. Os
  claims são feitos sequencialmente em uma sessão e não provam concorrência
  entre workers.
- `npm run verify:migrations`: passou (`107/107` artefatos baseline,
  `59/59` versões remotas, `56` migrations forward, nenhuma classificação
  desconhecida).
- O comando completo tentado foi
  `powershell -ExecutionPolicy Bypass -File scripts/verify-supabase-baseline.ps1
  -ApplyForwardMigrations -ExcludeTenantDataSeeds
  -PostMigrationVerification supabase/verification/ifood_mvp_foundation.sql`.
  Após a bridge, o replay local aplicou `20260911110000`,
  `20260911120000` e `20260916023512`; o verificador executou três enqueues e
  passou. O dump de schema/security e a configuração de plataforma
  permaneceram iguais ao baseline, e o lint terminou com exit 0.
- O teste focado do bridge e da fundação passou 9/9:
  `npx vitest run tests/zelomenuCanonicalModifierComponentsSchema.test.js
  tests/ifood.persistence-schema.test.js`.
- A tentativa inicial sem a bridge falhou em
  `20260911120000_zelomenu_canonical_pause.sql` com `column
  op.id_componente does not exist` (SQLSTATE `42703`); essa evidência é a
  causa-raiz que o reparo cobre.
- `supabase db advisors --linked` foi tentado em modo somente leitura e
  retornou `LegacyProjectNotLinkedError`. Mesmo autenticado, o catálogo
  vinculado não mostraria esta migration local ainda não aplicada.
- O harness executou somente replay em banco descartável local; nenhum `db
  push` ou `migration up` remoto, `db query` mutável contra o banco vinculado,
  `apply_migration`, deploy ou publicação foi executado.

## Task 4: Criar o processo dedicado do worker

**Files:**
- Create: `workers/ifood/index.js`
- Create: `workers/ifood/runtime.js`
- Create: `workers/ifood/config.js`
- Create: `workers/ifood/healthServer.js`
- Create: `workers/ifood/Dockerfile`
- Create: `workers/ifood/Dockerfile.dockerignore`
- Create: `tests/ifood.worker-runtime.test.js`
- Modify: `package.json`
- Modify: `.env.example`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `npm run worker:ifood`, `/health/live`, `/health/ready`, graceful shutdown e imagem independente.

- [x] **Step 1: Escrever testes RED do runtime**

Cobrir configuração ausente, loop sem sobreposição, abort em `SIGTERM`, readiness falso sem banco/lease e sanitização de erro.

- [x] **Step 2: Implementar runtime mínimo**

`runtime.js` recebe `integration`, `repository`, `clock` e `signal`; não importa Svelte nem `$env`. `config.js` lê somente `process.env` e valida com mensagens sem segredos.

```js
await runIfoodWorker({ integration, repository, signal: controller.signal });
```

- [x] **Step 3: Definir container**

Usar Node 24, usuário não-root, `HEALTHCHECK` no endpoint local e comando `node workers/ifood/index.js`. Copiar apenas package files, worker e módulos necessários.
O plano antigo citava `workers/ifood/.dockerignore`, mas o build usa contexto na
raiz do repositório; o arquivo efetivo é
`workers/ifood/Dockerfile.dockerignore`, nome reconhecido pelo Docker para o
ignore específico de `Dockerfile`.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/ifood.worker-runtime.test.js`

Run: `docker build -f workers/ifood/Dockerfile -t zelopdv-ifood-worker:test .`

Run: `docker run --rm zelopdv-ifood-worker:test node --version`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add dedicated iFood worker runtime`

**Resultado real (2026-09-16):**

- **Status: Concluída.** O processo dedicado é um módulo Node puro e injetável:
  `runtime.js` não importa Svelte nem `$env`, executa somente o probe seguro
  `repository.probeDependencies()` com resultado booleano
  `{databaseReachable, leaseCapable}`, e não chama HTTP iFood, claims,
  inbox ou processamento das Tasks 5/7. O bootstrap real usa uma dependência
  explícita `false/false`, portanto permanece `unready` até existir probe de
  produção verdadeiro; o adapter mock da Task 2 não é usado nem apresentado
  como pronto.
- RED comprovado: `npx vitest run tests/ifood.worker-runtime.test.js
  --reporter=verbose` falhou como esperado por módulos ausentes (0 testes
  coletados). GREEN focado: a mesma suíte passou com 1 arquivo e 13/13 testes
  aprovados, cobrindo defaults/precedência/URL/inteiros sem segredo, loop sem
  sobreposição, abort durante trabalho/sono, erros sanitizados, probe seguro,
  readiness stale/falha/shutdown, liveness independente, GET/HEAD/404/405,
  cleanup e ponte SIGTERM/SIGINT com drain.
- Suítes vizinhas:
  `npx vitest run tests/ifood.worker-runtime.test.js tests/ifood.domain.test.js
  tests/ifood.order-normalizer.test.js tests/onlineOrders.test.js
  tests/ifood.persistence-schema.test.js --reporter=verbose` passou 5 arquivos,
  55/55 testes. `npm run check` passou com 0 erros e 0 warnings.
- Suíte integral: `npm test -- --reporter=dot` passou 205 arquivos e 1.275
  testes; 3 arquivos e 3 testes condicionais foram pulados (208 arquivos,
  1.278 testes no total). Duração reportada pelo Vitest: 194,23 s.
- Container: `docker build -f workers/ifood/Dockerfile -t
  zelopdv-ifood-worker:test .` passou; o contexto ficou restrito ao package
  manifest/lock, worker e `src/lib/server/ifood` pelo
  `Dockerfile.dockerignore`. `docker run --rm zelopdv-ifood-worker:test node
  --version` retornou `v24.20.0`. `docker image inspect` confirmou
  `USER=node`, `CMD=["node","workers/ifood/index.js"]` e HEALTHCHECK local
  Node `fetch` em `/health/live`; o check não usa `/health/ready`, evitando
  restart-loop em indisponibilidade de banco.
- Smoke local do container com envs sintéticas: `/health/live` retornou
  `200 {"status":"ok","reason":"serving"}` e `/health/ready` retornou
  `503 {"status":"not_ready","reason":"dependencies_unavailable"}`,
  confirmando o fail-closed do bootstrap sem alegar disponibilidade real.
- O `worker:ifood` foi adicionado ao `package.json`; `.env.example` contém
  somente placeholders vazios e foi explicitamente liberado no `.gitignore`.
  Não houve `docker push`, deploy, publicação de imagem, alteração remota no
  Supabase, nem início das Tasks 5+.

## Task 5: Implementar autenticação e adapter HTTP de produção

**Files:**
- Create: `src/lib/server/ifood/adapters/httpIfoodAdapter.js`
- Create: `src/lib/server/ifood/http/tokenCache.js`
- Create: `src/lib/server/ifood/http/request.js`
- Create: `src/lib/server/ifood/http/rateLimit.js`
- Create: `tests/ifood.http-adapter.test.js`
- Modify: `workers/ifood/index.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** métodos `listMerchants`, `pollEvents`, `ackEvents`, `getOrder`, `confirm`, `startPreparation`, `readyToPickup`, `dispatch`, `getCancellationReasons`, `requestCancellation` e presença conforme o contrato da Task 1.

- [x] **Step 1: Escrever testes RED com `fetch` injetado**

Cobrir `expiresIn`, renovação antecipada, single-flight de token, um retry após `401`, `429` com reset, timeout, `5xx`, `202` sem confirmação e headers de merchant/polling.

- [x] **Step 2: Implementar transporte mínimo**

Não codificar duração de token nem limites numéricos do relatório. O adapter lê `expiresIn` e headers `X-RateLimit-*`; retries usam jitter e orçamento total.

- [x] **Step 3: Impedir vazamento**

O objeto de erro expõe status, código e retryability, mas nunca request headers, Authorization, clientSecret ou corpo integral.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/ifood.http-adapter.test.js tests/ifood.domain.test.js`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add production iFood HTTP adapter`

**Resultado real (2026-09-16):**

- **Status: Concluída.** `src/lib/server/ifood/http/tokenCache.js` implementa
  `client_credentials` contra
  `POST https://merchant-api.ifood.com.br/authentication/v1.0/oauth/token`
  (form-urlencoded `grantType`/`clientId`/`clientSecret`), sem duração fixa de
  token: lê `expiresIn` da resposta, renova antecipadamente dentro de uma
  margem configurável (padrão: piso de 5s ou 10% do tempo de vida, o que for
  maior, nunca mais que 90% do tempo de vida), compartilha uma única busca em
  voo entre chamadas concorrentes e expõe `invalidate()`.
- `src/lib/server/ifood/http/rateLimit.js` só reage a `Retry-After` e
  `X-RateLimit-Limit/Remaining/Reset`; nenhum limite numérico por endpoint foi
  fixado no código, pois o snapshot §8 documenta que nenhum `429` real foi
  observado nesta conta.
- `src/lib/server/ifood/http/request.js` resolve a URL base, injeta
  `Authorization: Bearer`, aplica timeout por `AbortSignal` (padrão 8s), faz
  exatamente um retry após `401` invalidando o token, e usa backoff
  exponencial com jitter (`random`/`sleep` injetados) limitado por um
  orçamento total (padrão 20s, no máximo 5 tentativas) para
  `429`/`5xx`/timeout/erro de rede; `4xx` não retryable falha na primeira
  tentativa sem retry. `IfoodHttpError` expõe somente
  `status`/`code`/`retryable`/`retryAfterMs?`; nunca headers de requisição,
  Authorization, `clientSecret`, `clientId`, token ou corpo da resposta —
  `message`/`stack` do `Error` também não carregam nenhum segredo.
- `src/lib/server/ifood/adapters/httpIfoodAdapter.js` implementa
  `listMerchants`, `getMerchantStatus` (somente leitura de presença, sem
  interrupções/horários — conforme a restrição da Task 1 §9),
  `pollEvents` (header `x-polling-merchants` com IDs concatenados por vírgula,
  `types`/`groups`/`categories` omitidos quando vazios, `204` vira `[]`),
  `ackEvents` (deduplica IDs, envia `[{id}]`, decompõe lotes acima de 10.000
  em múltiplos `POST` em vez de rejeitar o lote inteiro), `getOrder`,
  `confirm`, `startPreparation`, `readyToPickup`, `dispatch`,
  `getCancellationReasons`, `requestCancellation` e `requestOrderAction`
  compatível com o seam do adapter mock da Task 2. Toda ação com `202` retorna
  `{ orderId, action, accepted: true, status: 'accepted_http' }`, nunca uma
  confirmação comercial.
- Decisão de design registrada: `getOrder` com `404` vira `IfoodHttpError`
  com `code: 'IFOOD_HTTP_NOT_FOUND'` e `retryable: true` (nunca `null` nem
  exceção genérica), para o handler da Task 8 aplicar seu próprio backoff
  limitado de dez minutos em vez de tratar um detalhe ausente como falha
  fatal.
- `workers/ifood/config.js` ganhou leitura opcional de `IFOOD_CLIENT_ID` e
  `IFOOD_CLIENT_SECRET` (ambas juntas ou nenhuma); o par vira um campo
  `credentials` não enumerável (nunca aparece em `Object.keys`, `console.log`
  ou serialização do config) e só o booleano `hasIfoodCredentials` é
  enumerável. `workers/ifood/index.js` ganhou a fábrica aditiva
  `createIfoodHttpAdapterFromConfig(config, overrides)`; ela não é chamada
  pelo bootstrap padrão, o probe de dependências permanece `false/false` e
  nenhum polling foi ligado ao loop do worker — isso é escopo da Task 9.
- RED: `npx vitest run tests/ifood.http-adapter.test.js --reporter=verbose`
  falhou por módulo inexistente (0 testes coletados, `ERR_MODULE_NOT_FOUND`).
- GREEN focado: a mesma suíte passou 22/22 após a implementação mínima. Um
  desvio recuperável ocorreu no meio do processo: o teste de retry de `429`
  falhou porque o backoff do `request.js` usava apenas jitter aleatório em
  vez do `Retry-After` real (`sleep` chamado com `0` em vez de `1000`); o
  transporte foi corrigido para ler `Retry-After`/`X-RateLimit-Reset` via
  `parseRetryAfterMs` antes de recalcular o atraso, e a suíte voltou a
  22/22.
- GREEN com vizinhas:
  `npx vitest run tests/ifood.http-adapter.test.js tests/ifood.domain.test.js
  tests/ifood.worker-runtime.test.js tests/ifood.order-normalizer.test.js
  tests/ifood.persistence-schema.test.js tests/ifood.contract-fixtures.test.js
  --reporter=verbose` passou 6 arquivos e 82/82 testes, confirmando que a
  Task 4 e as anteriores continuam verdes sem alteração de teste existente.
- `npm run check` passou com 0 erros e 0 warnings (5.860 arquivos). `git diff
  --check` passou limpo para os arquivos novos (via `git add -N`) e
  modificados. A verificação manual de LF (`node -e` lendo os arquivos
  tocados) não encontrou `\r` em nenhum deles.
- A suíte de leakage prova que `JSON.stringify`, `String()`, `.message`,
  `.stack` e `Object.keys()` do erro nunca contêm `clientId`, `clientSecret`,
  o token de acesso, `Bearer` ou `Authorization`.
- Nenhuma chamada real ao iFood, deploy, push de imagem ou mutação no
  Supabase ocorreu nesta task; todos os testes usam `fetch` falso injetado.
  Conforme escopo explícito da task, a suíte integral (`npm test`) não foi
  executada; apenas os comandos listados acima.

**Revisão corretiva da Task 5 (2026-09-16, antes do commit):** o coordenador
revisou o adapter HTTP ainda não commitado e apontou quatro riscos; todos
foram corrigidos nos mesmos arquivos, sem commit adicional (a task ainda não
tinha sido commitada).

- **Retry ambíguo em POST não-idempotente.** `request.js` ganhou a opção
  `retryUnsafe` por chamada: um timeout, erro de rede ou `5xx` é ambíguo
  (não se sabe se o pedido foi processado do lado do iFood) e só é
  reautomatizado quando a chamada é idempotente. O padrão é `retryUnsafe:
  true` para `GET` e `false` para qualquer outro método; `confirm`,
  `startPreparation`, `readyToPickup`, `dispatch` e `requestCancellation`
  ficam no padrão `false` (uma falha ambígua vira erro imediato com
  `retryable: true`, sem nova tentativa automática — a Task 10 decide se
  reenviar com segurança), enquanto `ackEvents` passa `retryUnsafe: true`
  explicitamente porque reconfirmar um `id` já processado é inofensivo.
  `401` e `429` continuam retryable para qualquer método, pois são respostas
  explícitas (o pedido não foi processado do lado do servidor).
- **Timeout na busca de token.** `tokenCache.fetchToken` ganhou seu próprio
  `AbortController`/timeout (`timeoutMs`, padrão igual ao transporte:
  8000 ms), independente do timeout por recurso, porque uma busca de token
  travada bloquearia todo chamador que compartilha o single-flight. Timeout
  e falha de rede mapeiam para o mesmo `IFOOD_AUTH_FAILED` retryable, sem
  detalhe de requisição.
- **Espera vs. orçamento e cancelamento.** (a) O atraso de `429`
  (`Retry-After`/backoff) e o de `5xx`/timeout/rede agora só dormem quando o
  atraso calculado cabe no que resta do orçamento; se não cabe (ex.:
  `Retry-After` de 120 s contra um orçamento de 20 s), o erro
  `IFOOD_HTTP_RATE_LIMITED`/`IFOOD_HTTP_SERVER` sai imediatamente, com
  `retryAfterMs` quando aplicável, sem dormir o tempo todo. (b) Toda espera
  (`rateLimiter.waitForSlot` e o backoff) agora aceita o `signal` do
  chamador: `rateLimit.js` exporta `abortableSleep(ms, signal)` como `sleep`
  padrão, usado também como padrão em `request.js` e no adapter; abortar
  durante uma espera rejeita na hora com `IFOOD_HTTP_ABORTED`
  (`retryable: false`) em vez de aguardar o atraso inteiro.
- **`signal` opcional nos métodos do adapter.** Todos os métodos de
  `httpIfoodAdapter.js` aceitam um `signal` opcional (parâmetro de opções à
  direita para os métodos com id único, ou campo `signal` dentro do objeto
  de entrada para `pollEvents`/`requestOrderAction`/`requestCancellation`) e
  o repassam a `request()`, mantendo compatibilidade com a assinatura do
  seam mock — nenhum parâmetro existente mudou de posição ou tipo.

RED intencional não foi reexecutado como "módulo ausente" (a implementação
já existia); em vez disso, os 7 testes novos foram escritos para expor
exatamente os quatro riscos acima e comprovadamente passam apenas com a
correção aplicada (a lógica anterior não tinha `retryUnsafe`, timeout de
token, checagem de orçamento antes de dormir nem `signal` em `sleep`/
`waitForSlot`). Três asserts existentes (`toHaveBeenCalledWith` no
`sleep`/rate limiter) precisaram ser ajustados para `sleep.mock.calls[0][0]`
porque `sleep` passou a receber um segundo argumento (`signal`); nenhuma
asserção comportamental foi enfraquecida.

GREEN pós-correção:

- `npx vitest run tests/ifood.http-adapter.test.js --reporter=verbose`:
  29/29 (22 anteriores + 7 novos: timeout de token, POST ambíguo não
  reautomatizado, ACK ambíguo reautomatizado, `429` que excede orçamento sem
  dormir, aborto durante backoff, e as duas variantes de adapter para
  `confirm`/`ackEvents`).
- `npx vitest run tests/ifood.http-adapter.test.js tests/ifood.domain.test.js
  tests/ifood.worker-runtime.test.js --reporter=verbose`: 3 arquivos, 53/53.
- `npm run check`: 0 erros e 0 warnings (5.860 arquivos).
- `git diff --check`: limpo para os arquivos modificados e para os novos
  (`git add -N` seguido de `git diff --check`, depois `git reset`).
- Verificação manual de LF (`node -e`) confirmou ausência de `\r` em todos os
  arquivos tocados, incluindo os dois documentos.

## Task 6: Receber webhook assinado e persistir antes do `202`

**Files:**
- Create: `src/lib/server/ifood/webhookSignature.js`
- Create: `src/lib/server/ifood/inboxRepository.js`
- Create: `src/lib/server/ifood/webhookHandler.js` (não previsto originalmente —
  ver desvio no Resultado real: `+server.js` só pode exportar verbos HTTP)
- Create: `src/routes/api/integrations/ifood/webhook/+server.js`
- Create via CLI: `supabase/migrations/*_ifood_webhook_enqueue.sql` (desvio
  documentado no Resultado real — RPC que faltava para resolver
  `merchant_id → connection_id`)
- Create: `supabase/verification/ifood_webhook_enqueue.sql`
- Create: `tests/ifood.webhook-enqueue-schema.test.js`
- Create: `tests/api.ifood-webhook.test.js`
- Modify: `src/hooks.server.js` (rate limit da nova rota)
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`
- `.env.example` **não foi modificado**: já trazia `IFOOD_CLIENT_SECRET` da
  Task 4/5 e o arquivo não usa comentários em nenhuma linha.

**Produces:** endpoint público com HMAC-SHA256 constante, limite de corpo, inbox durável e `202` somente pós-commit.

- [x] **Step 1: Escrever testes RED**

Cobrir assinatura correta, byte alterado, assinatura ausente/malformada, JSON inválido, corpo excessivo, evento duplicado e falha de banco. Verificar que parsing ocorre depois da assinatura.

- [x] **Step 2: Implementar verificação sobre o stream cru**

Lido com um cap de bytes no próprio stream (não só `content-length`, que é
controlado pelo remetente); `verifyIfoodSignature` roda sobre os bytes
crus antes de qualquer parse. Comparar buffers do mesmo comprimento com
`timingSafeEqual`. Retornar `401` para assinatura inválida, `400/413` para
formato/tamanho, `503` se não houve commit e `202` para inserção, duplicata
ou merchant desconhecido/revogado já resolvidos pelo RPC.

- [x] **Step 3: Validar**

Run: `npx vitest run tests/api.ifood-webhook.test.js`

- [x] **Step 4: Atualizar tracker e commitar**

Commit: `feat: persist signed iFood webhooks`

**Resultado real (2026-09-16):**

- **Status: Concluída.** O contrato de assinatura foi confirmado via
  WebFetch nas páginas oficiais "Webhook — requisição", "Webhook — visão
  geral", "Webhook — boas práticas" e a página dedicada de assinatura
  (`webhook-signature`): header `X-IFood-Signature`, HMAC-SHA256 com o
  `clientSecret` do app sobre os bytes exatos do corpo (sem parse/normalização
  prévia), assinatura em **hex minúsculo sem prefixo**, comparação em tempo
  constante, resposta `202` esperada em até dois segundos (timeout de cinco),
  entrega at-least-once, e envelope de **um único evento por requisição**
  (não array) com campos `id`, `code`, `fullCode`, `createdAt`, `merchantId`,
  `orderId`. Nada ficou como "não confirmado": a codificação hex e a forma
  singular do envelope, que o plano listava como pontos a decidir
  conservadoramente se não confirmados, saíram confirmados nesta sessão.
- `src/lib/server/ifood/webhookSignature.js` implementa
  `verifyIfoodSignature(rawBytes, signatureHeader, secret)`: puro, sem
  `$env`, exige exatamente 64 chars hex (32 bytes de SHA-256) antes de
  decodificar, rejeita segredo vazio/não-string e assinatura ausente/não-string
  sem lançar, e só chama `timingSafeEqual` quando os dois buffers decodificados
  têm o mesmo tamanho.
- `src/lib/server/ifood/inboxRepository.js` expõe
  `createIfoodInboxRepository({ supabase })` com `enqueueWebhookEvent(event)`
  chamando a nova RPC via `.rpc(...).single()`; mapeia o `outcome` do banco
  (`inserted|duplicate|unknown_merchant`) para o vocabulário do repositório
  (`inserted|duplicate|ignored`) e nunca propaga o erro cru do driver (que
  pode ecoar parâmetros da chamada) — lança sempre uma mensagem genérica.
- `src/lib/server/ifood/webhookHandler.js` concentra o núcleo testável
  (`handleIfoodWebhook({ request, secret, repository })`,
  `decodeIfoodWebhookEvent`, `readBodyWithCap`, `jsonResponse` e as
  constantes `MAX_BODY_BYTES`/`SIGNATURE_HEADER`) e não importa `$env` nem
  `supabaseAdmin` — é puro. **Desvio corretivo pré-commit**: a primeira
  versão colocava esse núcleo dentro de `+server.js` com um export nomeado
  extra (`handleIfoodWebhook`); o coordenador apontou que o SvelteKit só
  aceita verbos HTTP e um punhado de exports reservados (`prerender`,
  `trailingSlash`, `config`, `entries`) em `+server.js`, e um export
  desconhecido gera "Invalid export" em dev/build. O núcleo foi extraído
  para `webhookHandler.js` (mesmo padrão dos outros módulos puros de
  `src/lib/server/ifood/`); `src/routes/api/integrations/ifood/webhook/+server.js`
  ficou só com `POST`, que lê `env.IFOOD_CLIENT_SECRET`, monta o repositório
  com `supabaseAdmin` e delega a `handleIfoodWebhook`. Guarda de exports:
  `node -e "...matchAll(/export\s+.../)..."` sobre o arquivo final imprime
  `[ 'POST' ]`. A mesma revisão removeu o fallback `process.env.IFOOD_CLIENT_SECRET`
  que a primeira versão copiou do padrão do webhook AbacatePay: um
  `grep -rn "process.env" src/routes/api` mostrou que só o próprio
  AbacatePay usa esse fallback (1 de ~19 rotas) e as demais 18 usam somente
  `$env/dynamic/private` — a convenção predominante do repo venceu, e a
  rota ficou só com `env.IFOOD_CLIENT_SECRET || null`.
  Ordem estrita dentro de `handleIfoodWebhook`: segredo ausente → `503`;
  `content-length` acima de 262144 (mesmo cap de
  `ifood_event_inbox_payload_size_check`) → `413` antes de ler; leitura do
  stream com corte físico no mesmo cap independentemente do header (cobre
  header ausente ou mentiroso); assinatura verificada sobre os bytes crus
  antes de qualquer `JSON.parse`; corpo inválido/UTF-8 malformado/campos
  obrigatórios ausentes (`id`, `merchantId`, `code`/`fullCode`) → `400`; só
  então persiste via repositório, e o `202` só é retornado depois do RPC
  resolver (inserido, duplicata ou merchant desconhecido/revogado). Toda
  resposta é JSON pequeno e genérico com `Cache-Control: no-store`; nenhuma
  resposta ecoa payload, ids, assinatura ou segredo (prova por teste
  dedicado). Código de evento desconhecido continua sendo persistido —
  quarentena é responsabilidade da Task 7.
- **Lacuna resolvida com nova migration** (gerada por
  `supabase migration new ifood_webhook_enqueue`, sem timestamp inventado):
  `supabase/migrations/20260916151846_ifood_webhook_enqueue.sql` cria
  `public.enqueue_ifood_webhook_event_v1(p_event_id, p_merchant_id,
  p_external_order_id, p_event_type, p_external_revision, p_occurred_at,
  p_payload)`, `SECURITY DEFINER`, `set search_path = ''`, mesmo contrato de
  detecção de `service_role`, `revoke all ... from public, anon,
  authenticated` e `grant execute ... to service_role`. A função resolve a
  conexão não revogada pelo `merchant_id` e, se não encontrar nenhuma,
  **retorna** `outcome='unknown_merchant'` sem inserir e sem lançar exceção
  (merchant desconhecido e conexão revogada seguem o mesmo caminho); quando a
  conexão existe, delega a inserção/duplicata para `enqueue_ifood_event_v1`
  da Task 3 (revisão de conformidade 2026-09-16, ver seção logo após o
  Resultado real da Task 6 — a versão original desta task duplicava o
  `insert`/`on conflict` em vez de reaproveitar a função da Task 3). **Política
  corrigida (revisão de conformidade 2026-09-16):** conexões `pending`,
  `active`, `degraded` e `paused` SÃO resolvidas e o evento É persistido —
  só uma conexão `revoked` ou um `merchant_id` que nunca chegou a existir em
  `ifood_internal.connections` caem em `unknown_merchant`. A reconciliação
  por polling da Task 9 só cobre merchants já conectados (mesma lista de
  conexões vivas usada pelo webhook); ela **não** faz backfill histórico
  para um merchant que nunca se conectou ao Zelo — esse caso está fora do
  escopo do MVP, não é uma lacuna que a Task 9 "fecha" depois. É por isso
  que a Task 17 agora exige criar a linha de conexão em `pending` **antes**
  de considerar o webhook/polling daquele merchant ativo (ver Task 17):
  assim nenhum evento de um merchant em processo de conexão é descartado
  como `unknown_merchant`.
- `supabase/verification/ifood_webhook_enqueue.sql` (novo arquivo
  transacional, com `ROLLBACK` no final) cobre: ACL negada para
  `anon`/`authenticated` e concedida para `service_role`; merchant
  desconhecido → `unknown_merchant` sem linha no inbox; conexão existente mas
  `revoked` → mesmo resultado; merchant ativo conhecido → `inserted`; mesmo
  `event_id` reenviado → `duplicate` apontando para o mesmo `inbox_id`; e
  que o evento inserido carrega o `empresa_id` da conexão resolvida (isolamento
  de tenant). `tests/ifood.webhook-enqueue-schema.test.js` (novo arquivo)
  prova por regex/substring que a migration contém a função, o hardening, os
  grants corretos, e que o branch `unknown_merchant` retorna antes do
  `insert into ifood_internal.event_inbox`.
- RED comprovado para os dois arquivos novos de teste: a migration foi
  esvaziada (backup em scratchpad, depois restaurada) e
  `npx vitest run tests/ifood.webhook-enqueue-schema.test.js --reporter=verbose`
  falhou 4/5 pelo motivo esperado (SQL vazio); os módulos JS/rota foram
  renomeados para `.bak` e
  `npx vitest run tests/api.ifood-webhook.test.js --reporter=verbose` falhou
  com `ERR_MODULE_NOT_FOUND` (0 testes coletados). Depois de restaurar os
  arquivos, `npx vitest run tests/ifood.webhook-enqueue-schema.test.js`
  passou 5/5 e `npx vitest run tests/api.ifood-webhook.test.js
  --reporter=verbose` passou 26/26, cobrindo: assinatura válida (`202
  inserted`), byte alterado (`401`, repositório não chamado), JSON inválido
  com assinatura errada (`401`, não `400` — prova de ordem
  verify-antes-de-parse, com `JSON.parse` espionado e nunca chamado),
  assinatura ausente/malformada/tamanho errado (`401`), JSON inválido com
  assinatura válida (`400`), campos obrigatórios ausentes (`400`),
  `content-length` acima do cap (`413`), corpo real acima do cap sem
  `content-length` ou com `content-length` mentindo para baixo (`413`),
  duplicata (`202`), merchant desconhecido/revogado (`202` sem erro),
  falha do repositório (`503`), segredo ausente (`503`, repositório nunca
  chamado), não-vazamento de payload/ids/assinatura/segredo no corpo da
  resposta em quatro cenários, e fiação real de `POST` com
  `$env/dynamic/private` + `supabaseAdmin` mockados (`503` sem
  `supabaseAdmin`; `202` fim-a-fim chamando `rpc('enqueue_ifood_webhook_event_v1',
  ...)`). Testes unitários de `verifyIfoodSignature` cobrem aceitar hex
  maiúsculo, rejeitar segredo errado, byte alterado, header ausente,
  não-hex e tamanho errado sem lançar.
- GREEN combinada: `npx vitest run tests/api.ifood-webhook.test.js
  tests/ifood.persistence-schema.test.js tests/ifood.webhook-enqueue-schema.test.js
  tests/ifood.http-adapter.test.js tests/api.abacatepay-webhook.test.js
  --reporter=verbose` passou 5 arquivos e 72/72 testes (nenhum teste
  pré-existente da Task 3/5 ou do webhook AbacatePay foi alterado).

**Revisão corretiva da Task 6 (2026-09-16, antes do commit):** o coordenador
apontou que `+server.js` só pode exportar verbos HTTP (e um punhado de
exports reservados do SvelteKit); o export nomeado extra
`handleIfoodWebhook` na primeira versão gera "Invalid export" em dev/build.
`handleIfoodWebhook`, `readBodyWithCap`, `decodeIfoodWebhookEvent`,
`jsonResponse` e as constantes foram movidos para o novo
`src/lib/server/ifood/webhookHandler.js` (puro, sem `$env`/`supabaseAdmin`);
`+server.js` ficou só com `POST`. A mesma revisão removeu o fallback
`process.env.IFOOD_CLIENT_SECRET`, copiado do padrão do webhook AbacatePay
sem checar a convenção predominante do repo (`grep -rn "process.env"
src/routes/api` mostra só 1 de ~19 rotas usando esse fallback; as outras 18
usam só `$env/dynamic/private`).

- `tests/api.ifood-webhook.test.js` foi ajustado para importar
  `handleIfoodWebhook` de `webhookHandler.js` (`loadWebhookHandler`) e
  `POST` da rota (`loadRoute`); nenhuma asserção mudou.
- Guarda de exports: `node -e "const s=require('fs').readFileSync('src/routes/api/integrations/ifood/webhook/+server.js','utf8'); const ex=[...s.matchAll(/export\s+(?:async\s+)?(?:function|const|let)\s+(\w+)/g)].map(m=>m[1]); console.log(ex)"`
  imprime `[ 'POST' ]`.
- `npx svelte-kit sync && npm run check`: `5867 FILES 0 ERRORS 0 WARNINGS`.
- Re-GREEN: `npx vitest run tests/api.ifood-webhook.test.js
  tests/ifood.webhook-enqueue-schema.test.js tests/ifood.persistence-schema.test.js
  --reporter=verbose` passou **3 arquivos e 37/37 testes** (26 + 6 + 5 —
  a contagem por arquivo não mudou, só a origem do import).
- `npm run verify:migrations`: inalterado, `107/107`/`59/59`/`57` forward.
  `git diff --check` limpo (via `git add -N` + reset) para os 11 arquivos
  tocados, incluindo o novo `webhookHandler.js`. Verificação manual de LF
  via `node -e` (byte `13`/CR) não encontrou `\r` em nenhum dos 11.
- Nenhum commit foi criado por esta revisão (a task ainda não tinha sido
  commitada).
- `npm run check`: `5866 FILES 0 ERRORS 0 WARNINGS`. `npm run
  verify:migrations`: `107/107` artefatos baseline, `59/59` versões remotas,
  agora **57** migrations forward (+1 desta task), nenhuma classificação
  desconhecida. `git diff --check` limpo para os arquivos novos (via
  `git add -N` + reset) e modificados. Verificação manual de LF via `node -e`
  (checando byte `13`/CR) não encontrou nenhum arquivo tocado com `\r`.
- O harness rodou com Docker Desktop disponível nesta sessão (duas execuções
  em background, pela duração do bootstrap descartável). A primeira tentativa
  com os dois arquivos separados por vírgula
  (`-PostMigrationVerification a.sql,b.sql`) falhou rápido: o parâmetro é
  `[string[]]`, e uma lista separada por vírgula em um só token vira uma
  string única que `Resolve-Path` não encontra — desvio de sintaxe do
  operador, não do schema. A segunda tentativa separou os dois arquivos por
  espaço (`-PostMigrationVerification a.sql b.sql`) e aplicou as duas
  migrations do iFood sem erro, mas o log
  (`post-migration verifiers passed: 1.`) e a ausência de qualquer
  `enqueue_ifood_webhook_event_v1` no `psql` mostraram que só o primeiro
  arquivo (`ifood_mvp_foundation.sql`) rodou; o binding de array do
  PowerShell 5.1 para este parâmetro não se comportou como esperado ao
  atravessar `powershell -File` de dentro de outra sessão PowerShell — não
  há indício de que seja um problema do schema/RPC novo. Uma terceira
  execução, isolada e só com
  `-PostMigrationVerification supabase/verification/ifood_webhook_enqueue.sql`,
  aplicou as mesmas duas migrations e rodou exatamente essa verificação via
  `psql ... -v ON_ERROR_STOP=1`, terminando com `exit code 0`,
  `BASELINE_VERIFIED cutoff=20260813091000` e dump de schema/segurança igual
  à produção — prova real de que a Task 6 replaya e verifica limpo no
  harness. O comparativo de schema/security/lint das três execuções não
  mudou (mesmo hash de dump normalizado). Nenhum `db push`/`migration up`
  remoto, mutação no banco vinculado, `apply_migration`, deploy ou
  publicação ocorreu; todas as três rodadas usaram apenas o banco Postgres
  descartável do harness.
- Conforme escopo explícito da task, a suíte integral (`npm test`) não foi
  executada; nenhuma chamada real ao iFood, deploy, push de imagem ou
  mutação no Supabase vinculado ocorreu nesta task. `.env.example` já trazia
  `IFOOD_CLIENT_SECRET` da Task 4/5 e o arquivo não usa comentários em
  nenhuma linha, então nada foi adicionado nele, conforme a instrução de só
  comentar se o arquivo já usasse comentários.

## Revisão de conformidade das Tasks 1–6 (2026-09-16)

Revisão pós-implementação fechando seis lacunas de verificação/conformidade
nas Tasks 1–6, antes de iniciar a Task 7. Trabalho feito só no worktree
`.worktrees/ifood-mvp` (branch `codex/ifood-mvp`); nenhum commit, push,
deploy ou chamada real ao iFood/Supabase vinculado.

**G1 — harness não rodava vários arquivos de verificação numa chamada.**
Problema: `-PostMigrationVerification a.sql,b.sql` falhava, e separado por
espaço só o primeiro arquivo rodava (`-File` colapsa o binding de array do
PowerShell 5.1 num único token). Correção: `scripts/verify-supabase-baseline.ps1`
agora normaliza `$PostMigrationVerification` fazendo split por vírgula em
cada entrada, trim, remoção de vazios e resolução de caminho relativo à raiz
do repo com erro claro se o arquivo não existir; comportamento de arquivo
único preservado. Evidência: rodada única do harness com
`-PostMigrationVerification supabase/verification/ifood_mvp_foundation.sql,supabase/verification/ifood_webhook_enqueue.sql`
terminou com `post-migration verifiers passed: 2` e `BASELINE_VERIFIED`
(`exit code 0`).

**G2 — leases da Task 3 nunca provadas sob concorrência real.**
Problema: o verificador da foundation reivindica sequencialmente numa única
sessão; não prova ausência de double-claim sob workers concorrentes de
verdade. Correção: novo `scripts/verify-ifood-lease-concurrency.mjs`, no
mesmo padrão de `verify-sale-owner-concurrency.mjs` (processos `psql`
independentes via `child_process.spawn`, aceitando só
`ZELOPDV_DISPOSABLE_DB_URL=postgresql://postgres:postgres@127.0.0.1:55322/postgres`).
Cria fixture (`empresa_perfil`, `connections`, `zelo_orders`, `order_refs`)
via superusuário, depois `set role service_role` por conexão (mesmo
contrato do resto do harness); enfileira 40 eventos via
`enqueue_ifood_event_v1`, insere 40 `order_commands` diretamente (viável
sem código da Task 10); 4 workers concorrentes disputam
`claim_ifood_events_v1`/`claim_ifood_commands_v1` até esvaziar a fila.
Assertions: total reivindicado == 40 em cada fila, nenhum id duplicado entre
workers, lease ids todos distintos; CAS provado em
`finish_ifood_event_v1`/`finish_ifood_command_v1` — lease estranho/forjado
rejeitado com `LEASE_LOST`, lease real finaliza com sucesso. Wired em
`-RunConcurrencyProbes` (junto às três probes existentes). Evidência (rodada
real do harness): `EVENT_CLAIM_EXACTLY_ONCE_PASS: 40 events, 4 workers, no
duplicate claim, 40 distinct lease ids.`, `EVENT_FINISH_CAS_PASS`,
`COMMAND_CLAIM_EXACTLY_ONCE_PASS: 40 commands, 4 workers, no duplicate
claim, 40 distinct lease ids.`, `COMMAND_FINISH_CAS_PASS` — todas passaram
junto com as três probes pré-existentes.

**G3 — `enqueue_ifood_webhook_event_v1` duplicava a lógica de insert da foundation.**
Problema: a RPC do webhook reimplementava o `insert`/`on conflict` de
`enqueue_ifood_event_v1` em vez de reaproveitá-la. Correção: reescrita de
`supabase/migrations/20260916151846_ifood_webhook_enqueue.sql` (aceitável —
migration nunca aplicada em nenhum banco compartilhado, estado documentado
no cabeçalho do arquivo) para resolver a conexão não revogada por
`merchant_id` e então delegar para `public.enqueue_ifood_event_v1(...)`,
mapeando `inserted` true/false para `outcome` `'inserted'`/`'duplicate'`,
mantendo `'unknown_merchant'` sem lançar. `tests/ifood.webhook-enqueue-schema.test.js`
atualizado para verificar a delegação (`from public.enqueue_ifood_event_v1(`)
em vez do `insert`/`on conflict` que não existe mais na migration. O check
de `service_role` em `enqueue_ifood_event_v1` (que lê `current_setting('role')`,
uma GUC de sessão) continua passando quando chamado de dentro da função
`SECURITY DEFINER` do webhook, porque `current_setting('role')` reflete
`SET ROLE` da sessão, não é resetado pela troca de privilégio do
`SECURITY DEFINER` — provado, não assumido: a mesma rodada do harness que
prova G1 rodou `supabase/verification/ifood_webhook_enqueue.sql` (que só
chama a RPC do webhook, nunca a de fundação diretamente) e passou
(`post-migration verifiers passed: 2`), incluindo os casos de
`inserted`/`duplicate`/`unknown_merchant`/isolamento de tenant.

**G4 — texto de política impreciso sobre merchant desconhecido.**
Problema: a doc dizia que o polling da Task 9 "recupera" eventos de webhook
de merchants desconhecidos; incorreto, porque a Task 9 só faz polling de
merchants já conectados. Correção: fatos corrigidos no cabeçalho da
migration `20260916151846_ifood_webhook_enqueue.sql`, na seção "Resultado
real" da Task 6 acima e em `docs/CURRENT.md` — conexões
`pending`/`active`/`degraded`/`paused` SÃO persistidas (só `revoked` ou
merchant nunca conectado caem em `unknown_merchant`); eventos de um
merchant que nunca se conectou ao Zelo estão fora do escopo do MVP (sem
backfill histórico). Adicionado requisito explícito ao Step 2 da Task 17:
a linha de conexão precisa existir em `pending` antes do
webhook/polling daquele merchant ser considerado ativo, para nenhum evento
de um merchant em processo de conexão ser descartado como
`unknown_merchant`. `src/lib/server/ifood/inboxRepository.js` e
`webhookHandler.js` já não continham a alegação errada (checado por
`grep`); nenhuma mudança de código foi necessária ali.

**G5 — adapter HTTP não era seam-compatível com `createIfoodIntegration`.**
Problema: `createIfoodIntegration.connectMerchant` chama
`adapter.connectMerchant`, ausente em `httpIfoodAdapter`; `integration.connectMerchant`
lançava `TypeError: iFood adapter does not implement connectMerchant` contra
o adapter real (funcionava só contra o mock). Correção:
`src/lib/server/ifood/adapters/httpIfoodAdapter.js` ganhou
`connectMerchant({ merchantId, signal })`, retornando o mesmo formato do
mock (`{ merchantId, connected }`). `getConnectionHealth` foi deixado
ausente de propósito e documentado: o adapter não tem acesso a repositório
para mapear `empresaId → merchantId`, e a seam já trata a função como
opcional (fallback `{ empresaId, status: 'unknown' }`).

**Correção pós-revisão (mesmo dia):** a primeira versão de `connectMerchant`
lia `listMerchants` (`GET /merchants`) e checava se o `merchantId` aparecia
na página retornada — mas para um app centralizado essa lista contém todo
merchant que já autorizou o Zelo, então pode paginar facilmente; um
merchant na página 2+ seria reportado `connected:false` erroneamente. A
página oficial que documenta a paginação retorna `403` para fetchers,
então os params reais não puderam ser confirmados (nem foram inventados).
Correção: `connectMerchant` passou a usar a rota por-merchant que o
snapshot efetivamente exercitou (`GET /merchants/{id}/status`,
`200` observado no §3): `2xx` → `{ merchantId, connected: true }`; `403`
ou `404` (`IfoodHttpError.status`) → `{ merchantId, connected: false }`;
qualquer outro erro (`401` após retry, `429`, `5xx`, timeout, abort)
propaga sem alteração, para uma indisponibilidade nunca ser confundida com
"não conectado". `listMerchants` continua página única; seu comentário foi
trocado para registrar que a paginação é real mas não confirmada (sem
inventar `page`/`size`), e a mesma nota foi ao `CONTRACT_SNAPSHOT.md` §3.

Novo `tests/ifood.adapter-seam.test.js` roda as mesmas operações de
`createIfoodIntegration` (`connectMerchant`, `requestOrderAction`,
`reconcileEvents`) contra o mock e contra `httpIfoodAdapter` com fetch
fake, incluindo um bloco dedicado de mapeamento de status
(`200`→`true`, `403`→`false`, `404`→`false`, `503`→lança). RED comprovado
antes da correção original (sem `connectMerchant` nenhum): `git stash`
isolado só do `httpIfoodAdapter.js` e
`npx vitest run tests/ifood.adapter-seam.test.js` falhou 1/6 com
exatamente o `TypeError` esperado; restaurado o stash (sha conferido antes
do drop), a suíte completa voltou a passar 6/6.

Validação da correção pós-revisão (só JS de adapter e testes mudaram; sem
harness nem suíte integral, conforme instrução do coordenador):
`npx vitest run tests/ifood.http-adapter.test.js tests/ifood.adapter-seam.test.js
tests/ifood.domain.test.js` → **3 arquivos, 54/54 testes** (33 + 10 + 11;
o adapter test ganhou 4 casos novos de `connectMerchant` — 200/403/404/503 —
e o seam test ganhou um bloco de 4 casos de mapeamento de status, saindo de
6 para 10 testes). `npm run check` → `5868 FILES 0 ERRORS 0 WARNINGS`.
`git diff --check` (via `git add -N` dos 10 arquivos tocados, depois
`git reset`) → limpo. Varredura de LF via `node` checando byte `13`/CR nos
10 arquivos tocados → nenhum `\r` encontrado. Nenhum `git stash` foi usado
nesta correção pós-revisão (só na comprovação de RED original, já
finalizada e com a stash já dropada antes desta mensagem do coordenador).

**G6 — container do worker não revalidado após a Task 5 mudar os imports.**
Problema: `workers/ifood/index.js` importa
`src/lib/server/ifood/adapters/httpIfoodAdapter.js` (que agora ganhou
`http/tokenCache.js`, `http/rateLimit.js`, `http/request.js` como
dependências transitivas da Task 5), sem revalidação do container desde
então. Correção: nenhuma mudança de `Dockerfile`/`Dockerfile.dockerignore`
foi necessária — o ignore já libera toda a árvore
`src/lib/server/ifood/**`. Evidência: `docker build -f workers/ifood/Dockerfile
-t zelopdv-ifood-worker:test .` completou sem erro (grafo de imports resolve
dentro da imagem); `docker run --rm zelopdv-ifood-worker:test node --version`
→ `v24.20.0`; smoke com envs sintéticas
(`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `IFOOD_CLIENT_ID`,
`IFOOD_CLIENT_SECRET` sintéticos, exigidos por `workers/ifood/config.js`)
→ `GET /health/live` `200 {"status":"ok","reason":"serving"}` e
`GET /health/ready` `503 {"status":"not_ready","reason":"dependencies_unavailable"}`,
igual ao comportamento documentado na Task 4. Container e imagem removidos
ao final (`docker rm -f`, `docker rmi`).

**Validação combinada:**
- `npx vitest run tests/ifood.*.test.js tests/api.ifood-webhook.test.js
  tests/onlineOrders.test.js` → **10 arquivos, 143/143 testes** após a correção
  de paginação do G5 (inclui o novo `tests/ifood.adapter-seam.test.js`, 10/10, e
  `tests/ifood.webhook-enqueue-schema.test.js` atualizado, 5/5).
- `npm run check` → `5868 FILES 0 ERRORS 0 WARNINGS`.
- `npm run verify:migrations` → `107/107` baseline, `59/59` remoto, `57`
  forward (inalterado — só o conteúdo de uma migration não aplicada mudou).
- `git diff --check` (via `git add -N` dos 8 arquivos tocados/novos, depois
  `git reset`) → limpo, sem erros de whitespace.
- Varredura de LF via `node` checando byte `13`/CR em cada um dos 8 arquivos
  tocados/novos → nenhum `\r` encontrado.
- Harness único:
  `powershell -ExecutionPolicy Bypass -File scripts/verify-supabase-baseline.ps1
  -ApplyForwardMigrations -ExcludeTenantDataSeeds -RunConcurrencyProbes
  -PostMigrationVerification supabase/verification/ifood_mvp_foundation.sql,supabase/verification/ifood_webhook_enqueue.sql`
  → `exit code 0`, `BASELINE_VERIFIED cutoff=20260813091000`,
  `post-migration verifiers passed: 2`, e as quatro probes de
  `-RunConcurrencyProbes` passaram: as três já existentes
  (`verify-customer-identity-concurrency`, `verify-whatsapp-confirmation-concurrency`,
  `verify-sale-owner-concurrency`) e a nova `verify-ifood-lease-concurrency`,
  todas sem tocar o banco vinculado.
- `npm test -- --reporter=dot` (suíte integral, uma vez) → **209 arquivos
  aprovados, 3 pulados (212); 1.341 testes aprovados, 3 pulados (1.344)**;
  duração 154,44s (execução anterior aos 8 testes adicionados na correção de
  paginação do G5, validados na suíte iFood acima). Nenhum teste pré-existente foi alterado além dos citados
  acima.
- G6 (Docker) documentado acima.


## Itens externos autorizados pelo dono (2026-09-16)

Autorização explícita do dono em chat para executar o que estava em aberto
após a conformidade das Tasks 1–6.

- **Migrations no Supabase vinculado (`xnnjyrblpvsqrtsshawa`): aplicadas.**
  Pré-checagem read-only: `ifood_internal` inexistente; o check atual de
  `zelo_orders.source` era subconjunto do novo (só acrescenta `ifood`); 277
  pedidos com `zelomenu`/`manual`/`legacy_zelochat`; o contrato ZeloMenu
  da bridge `20260911110000` já existia (bridge não aplicada, desnecessária
  em produção). Aplicadas via MCP `apply_migration` sem o `begin/commit`
  externo: `ifood_mvp_foundation` (versão remota `20260916160223`) e
  `ifood_webhook_enqueue` (`20260916160236`); como nas migrations
  anteriores, a versão remota difere do timestamp do arquivo local.
  Pós-checagem: 6 tabelas com RLS; `anon`/`authenticated` sem USAGE no
  schema e sem EXECUTE nas 6 RPCs; `service_role` com EXECUTE; todas
  `SECURITY DEFINER`; check de `source` com `ifood`; 277 pedidos
  preservados. Smoke transacional com `set local role service_role` e
  `rollback`: merchant inexistente → `unknown_merchant`. Security advisor:
  objetos iFood só aparecem como INFO `rls_enabled_no_policy` (intencional,
  acesso exclusivo do service role); nenhum WARN novo.
- **Adapter HTTP contra a API real: executado (somente leitura).** Detalhe em
  `docs/integrations/ifood/CONTRACT_SNAPSHOT.md` → "Prova ao vivo do adapter
  HTTP": token, merchants, status (`200` autorizado / `403` não autorizado),
  polling `204`; sem headers `X-RateLimit-*`.
- **Comandos Order reais: executados** com dois pedidos gerados no Portal do
  Desenvolvedor pelo navegador do dono (sessão já autenticada). `getOrder`,
  motivos, `confirm`, `startPreparation`, `dispatch`, `readyToPickup` e
  `requestCancellation` retornaram `200`/`202` e cada um produziu o evento
  correspondente; ACK removeu os lotes. Tabela em
  `CONTRACT_SNAPSHOT.md` → "Ciclo completo com pedidos de teste".
- **Webhook real: executado ponta a ponta.** URL pública temporária foi um
  deploy Preview na Vercel do próprio `codex/ifood-mvp`
  (`.../api/integrations/ifood/webhook`), com `IFOOD_CLIENT_SECRET` e
  `SUPABASE_SERVICE_ROLE_KEY` do dono configurados só nesse ambiente, e a
  Vercel Authentication do preview desligada só durante o teste (religada em
  seguida — a extensão do navegador bloqueou a mim mesmo tentar desligá-la,
  como esperado pelo próprio guard de segurança do harness, e o dono fez essa
  parte manualmente). Um pedido de teste gerado com Status=`true` entregou
  `PLACED` por HTTP real com `X-IFood-Signature` válida; nosso servidor
  respondeu `202`, e como a loja de teste não tem `ifood_internal.connections`,
  o resultado foi `unknown_merchant`/`ignored` — `202` sem gravar nada,
  confirmado por leitura direta no Postgres. Um KEEPALIVE de "Testar conexão"
  também chegou com assinatura válida, mas `400` por não ter `merchantId`
  (fora do escopo da Task 6, ver `CONTRACT_SNAPSHOT.md` §9). Webhook
  desligado e proteção do preview restaurada ao final. Detalhe completo em
  `CONTRACT_SNAPSHOT.md` → "Webhook real exercitado ponta a ponta".
- **Não executável por este agente (bloqueio externo, não de código):**
  - homologação iFood: processo conduzido pelo iFood sobre o fluxo completo
    (Tasks 7–20 ainda pendentes).

## Task 7: Processar inbox com lease, retry e dead-letter

**Files:**
- Create: `src/lib/server/ifood/inboxProcessor.js`
- Create: `src/lib/server/ifood/retryPolicy.js`
- Create: `tests/ifood.inbox-processor.test.js`
- Modify: `workers/ifood/runtime.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `runInboxCycle`, classificação `retryable|terminal|quarantine`, backoff limitado e renovação segura de lease.

- [x] **Step 1: Escrever testes RED**

Cobrir crash após claim, crash após efeito e antes de finish, dois workers, erro terminal, jitter determinístico, payload desconhecido e limite de tentativas.

- [x] **Step 2: Implementar ciclo pequeno**

O processor chama `claim`, processa sequencialmente por pedido externo para preservar ordem e finaliza com CAS do lease. Exceção não derruba o processo inteiro.

- [x] **Step 3: Validar**

Run: `npx vitest run tests/ifood.inbox-processor.test.js tests/ifood.worker-runtime.test.js`

- [x] **Step 4: Atualizar tracker e commitar**

Commit: `feat: process durable iFood event inbox`

**Resultado real (2026-09-16):**

- **Status: Concluída.** `src/lib/server/ifood/retryPolicy.js` exporta
  `computeNextAttemptDelayMs({ attempts, random, baseMs, capMs })` (backoff
  exponencial com dobra por tentativa, jitter uniforme via `random`
  injetado, mesmo idioma de `src/lib/server/ifood/http/request.js`'s
  `backoffDelayMs`) e `computeNextAttemptAt({ attempts, clock, random,
  baseMs, capMs })`, que converte o atraso num ISO-8601 absoluto via um
  `clock` injetado. Os defaults (base 1s, cap 10min) são escolha própria
  deste módulo — nada no snapshot do contrato ou nas RPCs fixa esse número
  — documentados no próprio arquivo e sobrescrevíveis por opção; não são
  mágicos sem override.
- `src/lib/server/ifood/inboxProcessor.js` exporta
  `createIfoodInboxProcessor({ repository, handler, workerId, limit=10,
  leaseSeconds=120, retryPolicy, clock, random, logger })` com um único
  método público `runInboxCycle({ signal } = {})`. O `repository` é o seam
  de exatamente dois métodos assíncronos documentados via JSDoc no próprio
  arquivo: `claimEvents({ workerId, limit, leaseSeconds, signal })` →
  array de linhas no formato exato de `claim_ifood_events_v1`
  (`inboxId, eventId, connectionId, empresaId, merchantId,
  externalOrderId, eventType, externalRevision, occurredAt, payload,
  attempts, leaseId, leaseUntil, status`) e `finishEvent({ inboxId,
  leaseId, outcome, errorCode, errorMessage, nextAttemptAt, signal })` →
  resolve ou rejeita (ex.: lease perdida/`LEASE_LOST`, que a RPC real
  levanta como exceção quando `finish_ifood_event_v1` não encontra a
  combinação `id`/`lease_id` em `processing`). **Nenhum repositório real
  Supabase foi criado nem ligado a `workers/ifood/index.js`** — a fábrica
  de bootstrap padrão continua exatamente tão fail-closed quanto nas Tasks
  4–6 (prova: teste `uses only the non-mutating dependency probe in Task 4`
  continua verde sem alteração, e o novo teste `does not invoke processInbox
  at all when it is not supplied` cobre o caminho padrão). Isso fica para
  uma task futura, no mesmo padrão adiado que as Tasks 4–6 já registraram
  para o probe/adapter reais.
- `handler(event, context)` é a função injetada (o futuro handler de
  projeção de pedidos canônicos da Task 8) e deve resolver em `{ outcome:
  'processed' | 'retryable' | 'terminal' | 'quarantine', errorCode?,
  errorMessage?, nextAttemptAt? }`. Esses quatro literais já são,
  intencionalmente, um subconjunto exato dos sete valores que
  `finish_ifood_event_v1` aceita (`processed`, `retryable`,
  `failed_retryable`, `terminal`, `failed_terminal`, `quarantine`,
  `dead_letter`), então o processor repassa o outcome do handler para
  `finishEvent` sem reescrevê-lo — nunca inventa um literal novo que a RPC
  não aceitaria.
- **Nuance do contrato, documentada em comentário no próprio
  `inboxProcessor.js` e coberta por teste dedicado**:
  `finish_ifood_event_v1` (migration aplicada em produção
  `supabase/migrations/20260916023512_ifood_mvp_foundation.sql`, **não
  modificada nesta task**) tem este `case` interno:
  `p_outcome = 'processed' → status 'processed'`;
  `p_outcome in ('retryable','failed_retryable') → status
  'failed_retryable'`; qualquer outro valor válido (`terminal`,
  `failed_terminal`, `quarantine`, `dead_letter`) → status
  `'dead_letter'`. Ou seja, `terminal` e `quarantine` produzem a **mesma**
  linha `status = 'dead_letter'` na tabela — não existe um status
  `quarantine` separado no banco. O teste `handler returns terminal and
  quarantine: both call finishEvent with their outcome string` prova que o
  processor preserva os dois outcomes distintos na chamada a `finishEvent`
  (`terminal`/`quarantine`) e que só o `errorCode` (ex.:
  `'quarantine_unknown_event'` vs `'terminal_error'`) permite depois
  diferenciar por que um evento foi parar em `dead_letter`. Isso é
  comportamento existente e intencional da RPC já aplicada; não deve ser
  "corrigido" tocando a migration.
- `claim_ifood_events_v1` já move para `dead_letter`, antes de reivindicar
  qualquer linha, tudo cujo `attempts >= max_attempts` (tanto
  `queued`/`failed_retryable` quanto `processing` com lease expirada). O
  processor não reimplementa essa eviction; só calcula um
  `next_attempt_at` limitado via `retryPolicy` quando o outcome é
  `retryable` e o handler não forneceu um.
- Dentro de `runInboxCycle`, as linhas reivindicadas são processadas
  **sequencialmente**, na ordem retornada por `claimEvents` (que já vem
  ordenada por `next_attempt_at, received_at, id` na RPC real) — nunca em
  paralelo. Isso preserva ordem relativa entre eventos do mesmo pedido
  externo dentro de um ciclo de claim. Provado pelo teste `processes
  claimed rows sequentially, in the order returned by claimEvents`, que
  intercala `start:`/`end:` por linha com um `setTimeout` artificial no
  handler e afirma que não há sobreposição.
- Se o `handler` lança exceção, ou resolve com um `outcome` desconhecido
  (incluindo campo ausente), o processor trata como `retryable` com um
  `errorCode` genérico e estável (`'HANDLER_ERROR'` para exceção,
  `'HANDLER_INVALID_OUTCOME'` para outcome malformado) e uma
  `errorMessage` genérica — a mensagem real do erro lançado **nunca**
  aparece em nenhum argumento passado a `finishEvent` nem em nenhuma
  chamada de `logger`. Provado pelo teste `handler throws` (mensagem
  secreta simulando dado sensível, verificada por `not.toMatch`/`not
  .toContain` em toda chamada e em todo log).
- Se `finishEvent` lança (ex.: `LEASE_LOST` porque outro worker já
  reivindicou a lease expirada, ou qualquer erro transitório de banco), o
  processor captura, loga de forma genérica via `logger?.error` (nunca o
  texto do erro) e **continua** para a próxima linha reivindicada — uma
  falha de finish nunca aborta o ciclo inteiro nem escapa de
  `runInboxCycle`. Provado pelo teste `finishEvent throws for one row`.
- `runInboxCycle` retorna sempre um resumo `{ claimed, processed, retried,
  deadLettered, finishFailed }` — só contagens e nunca payload, dados de
  merchant/cliente ou texto de erro bruto (provado pelo teste `never logs
  or surfaces payload contents in the summary object`).
- Se `signal` já está abortado antes do início de `runInboxCycle`, o
  método retorna o resumo vazio **sem chamar `claimEvents`** — decisão
  deliberadamente simples (não há trabalho parcial a desfazer, porque
  nada foi reivindicado ainda). Provado pelo teste `returns trivially
  without calling claimEvents when signal is already aborted`.
- Teste de duas instâncias de `createIfoodInboxProcessor` compartilhando um
  único repositório fake, cujo `claimEvents` simula a semântica real de
  `SKIP LOCKED` (uma linha já reivindicada por uma chamada não pode ser
  reivindicada de novo até a lease expirar — simulado aqui como "nunca"
  dentro do teste) prova que rodar os dois `runInboxCycle()`
  concorrentemente (`Promise.all`) reivindica e finaliza cada uma das
  quatro linhas exatamente uma vez no total, e que a soma dos resumos de
  cada instância bate com o que ela de fato reivindicou — a camada de
  orquestração não duplica claim por conta própria, além da garantia que o
  `SKIP LOCKED` real do banco já daria.
- `workers/ifood/runtime.js` ganhou um hook aditivo e opcional
  `options.processInbox`: um callback assíncrono no formato de
  `runInboxCycle`, invocado uma vez por ciclo logo após o probe/health
  existentes, só quando fornecido — o bootstrap padrão de
  `workers/ifood/index.js` (`createUnreadyWorkerDependencies`) nunca o
  fornece, então o caminho padrão fica byte-a-byte igual ao das Tasks 4–6
  (nenhum teste pré-existente de `tests/ifood.worker-runtime.test.js` foi
  alterado). O hook respeita `signal` (é rastreado por `activeOperations`
  e cancelável do mesmo jeito que o probe) e qualquer erro que lance passa
  pelo mesmo `reportError`/`sanitizeWorkerError` já usado pelo probe —
  nunca derruba o loop nem vaza texto de erro cru. Três testes novos
  cobrem: invocação uma vez por ciclo com `{ signal }`, ausência total de
  chamada quando a opção não é fornecida (prova que o default não mudou),
  e sanitização de uma falha do `processInbox` sem crashar o loop.
- RED: `npx vitest run tests/ifood.inbox-processor.test.js
  --reporter=verbose` falhou como esperado por módulo inexistente
  (`ERR_MODULE_NOT_FOUND` em `retryPolicy.js`, 0 testes coletados).
- GREEN focado: `npx vitest run tests/ifood.inbox-processor.test.js
  --reporter=verbose` passou **14/14** (3 de `retryPolicy` + 11 de
  `createIfoodInboxProcessor`).
- GREEN combinado (comando exato do Step 3, mais persistência):
  `npx vitest run tests/ifood.inbox-processor.test.js
  tests/ifood.worker-runtime.test.js tests/ifood.persistence-schema.test.js
  --reporter=verbose` passou **3 arquivos e 36/36 testes** (14 + 16 + 6 — as
  16 do worker-runtime incluem os 3 testes novos do hook `processInbox`
  além dos 13 já existentes das Tasks 4–6, nenhum alterado).
- `npm run check`: `5871 FILES 0 ERRORS 0 WARNINGS`. `git diff --check`
  (via `git add -N` para os arquivos novos, depois `git reset`) passou
  limpo para os 5 arquivos tocados. Verificação manual de LF via `node -e`
  (checando byte `13`/CR) não encontrou `\r` em nenhum dos 5.
- Nenhuma migration foi criada, editada ou reaplicada nesta task; nenhum
  repositório real Supabase foi escrito; nenhuma chamada real ao iFood;
  `npm test` integral não foi executado (fora do escopo pedido — apenas os
  arquivos iFood-adjacentes e os dois vizinhos de segurança foram
  rodados).

## Task 8: Buscar detalhes e projetar pedidos em `zelo_orders`

**Files:**
- Create via CLI: `supabase/migrations/*_ifood_canonical_order_projection.sql`
- Create: `supabase/verification/ifood_canonical_order_projection.sql`
- Create: `tests/ifood.canonical-projection-schema.test.js`
- Create: `tests/ifood.event-handler.test.js`
- Create: `src/lib/server/ifood/eventHandler.js`
- Modify: `src/lib/server/ifood/createIfoodIntegration.js`
- Modify: `src/lib/server/ifood/inboxProcessor.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `project_ifood_order_event_v1(jsonb)` service-role-only, referência externa única e detalhes com retry de `404` por no máximo dez minutos.

- [x] **Step 1: Gerar migration e escrever testes RED**

Requisito observado ao vivo (2026-09-16): `DELIVERY_DROP_CODE_REQUESTED` e
`CANCELLATION_REQUESTED` são códigos reais fora do conjunto canônico; devem ser
registrados como eventos informativos processados, sem transição comercial e
sem ficar presos em quarentena/dead-letter.

Provar criação única para evento duplicado, não regressão para evento antigo, atualização de snapshot, preservação de desconhecido e isolamento tenant/merchant.

- [x] **Step 2: Implementar handler e projeção transacional**

O handler resolve conexão ativa, busca detalhe quando necessário, normaliza via Task 2 e chama uma única RPC transacional. `source` é `ifood`; comprador permanece somente no JSON snapshot e não gera `pessoas`.

- [x] **Step 3: Implementar retry de detalhe**

`404` dentro da janela vira retry com backoff; após dez minutos vira pendência administrativa. Outros `4xx` terminais não geram loop.

- [x] **Step 4: Validar JS e SQL**

Run: `npx vitest run tests/ifood.event-handler.test.js tests/ifood.canonical-projection-schema.test.js`

Run: `powershell -ExecutionPolicy Bypass -File scripts/verify-supabase-baseline.ps1 -VerificationFile supabase/verification/ifood_canonical_order_projection.sql`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: project iFood orders into canonical queue`

**Resultado real (2026-09-16):**

- **Status: Concluída no harness descartável local; migration não aplicada
  ao Supabase vinculado (`xnnjyrblpvsqrtsshawa`)** — essa aplicação fica a
  critério do coordenador após revisão linha a linha, conforme instrução
  desta task.
- Migration gerada pelo comando obrigatório
  `supabase migration new ifood_canonical_order_projection`, sem
  timestamp/nome inventado, em
  `supabase/migrations/20260916195009_ifood_canonical_order_projection.sql`.
  Cria só a função `public.project_ifood_order_event_v1(text, text, text,
  text, timestamptz, jsonb)` (`SECURITY DEFINER`, `set search_path = ''`,
  detecção de `service_role` idêntica ao padrão vigente, `revoke all` de
  `public, anon, authenticated`, `grant execute` só a `service_role`).
  Nenhum índice novo foi necessário: `ifood_order_refs_merchant_order_unique`
  (Task 3) já cobre o padrão de busca `(merchant_id, external_order_id)`.
  As tabelas/constraints/grants de `zelo_orders` e das demais tabelas
  centrais não foram tocadas.
- **`projectOrderEvent` (seam JS documentado em
  `src/lib/server/ifood/eventHandler.js` via JSDoc) e a RPC SQL usam o mesmo
  vocabulário de outcome:** `applied` | `ignored_duplicate` |
  `ignored_stale` | `quarantined_terminal_conflict` | `unknown_merchant`.
  Argumentos da RPC: `p_merchant_id, p_external_order_id, p_event_id,
  p_external_status, p_occurred_at, p_order` (o `p_order` é exatamente o
  objeto que `normalizeIfoodOrder` retorna — `customerSnapshot`,
  `fulfillment`, `payment`, `totals`, `items`). Retorna `(outcome,
  zelo_order_id, revision)`. **O orquestrador deve validar este vocabulário
  contra as necessidades futuras das Tasks 9/10/12/14** antes de aplicar a
  migration em qualquer banco: em particular, a Task 10 (comandos
  assíncronos) e a Task 14 (materializar venda em `CONCLUDED`) vão precisar
  ler `zelo_order_id`/`revision` a partir daqui, e a Task 12 (mapeamento de
  produto) vai precisar que `zelo_order_items.product_id` continue `null`
  para itens iFood até que exista vínculo explícito — este RPC já garante
  isso (nunca resolve `product_id`).
- **Decisão de design (rank/compare-and-set): SQL-autoritativo apenas.**
  `project_ifood_order_event_v1` tranca `ifood_internal.order_refs` e depois
  a linha correspondente de `zelo_orders` (`for update`, nessa ordem) e
  recalcula, como `CASE` interno, a mesma tabela de rank de 7 entradas que
  `eventPolicy.js`/`IFOOD_STATUS_RANK` já usa no domínio (Task 2). O handler
  em JS **não** duplica essa lógica contra uma leitura especulativa de
  `order_refs`: ele só decide, antes de qualquer chamada à RPC ou fetch de
  detalhe, os dois curto-circuitos que nunca alcançam o banco — código
  informativo (`processed` imediato) e código fora do conjunto canônico de
  7 (`quarantine` imediato). Motivo registrado: manter duas cópias
  independentes do rank table arriscaria divergência silenciosa se um
  status novo fosse adicionado a uma e não à outra; a única leitura
  corretude-crítica do rank table acontece dentro da mesma transação que
  escreve a linha, então ela mora só ali.
- **Retry de detalhe (`404`):** ancorado no `occurredAt` do próprio evento
  (não no `attempts` do worker, que varia com jitter/backoff/reinícios) —
  dentro de 10 minutos vira `retryable` com `nextAttemptAt` calculado por
  `retryPolicy.computeNextAttemptAt` (Task 7, injetado); passado esse
  tempo, `terminal` com `errorCode: 'ORDER_DETAIL_NOT_FOUND_TIMEOUT'`. Sem
  `occurredAt` utilizável (não deveria acontecer dado o contrato das Tasks
  1–7, mas o handler falha fechado): teto conservador de 3 tentativas em
  vez de retry infinito, documentado no próprio arquivo. Qualquer outro
  erro do adapter com `retryable: true` vira `retryable` genérico; um erro
  não-retryable vira `terminal`; a própria RPC falhando (erro de banco
  simulado) vira `retryable` com `errorCode: 'PROJECTION_RPC_ERROR'`
  genérico — a mensagem bruta do erro nunca é repassada a `finishEvent` nem
  aparece em nenhum campo do resultado (provado por teste dedicado com uma
  mensagem secreta simulando dado sensível).
- **Simplificação `additionalFees`→`delivery_fee`:** `zelo_orders` não tem
  coluna própria para taxas adicionais, então
  `delivery_fee := totals.deliveryFee + totals.additionalFees` e
  `discount := totals.benefits`. **`total` é calculado em SQL como
  `subtotal + delivery_fee - discount`** (nunca copiado de
  `totals.orderAmount` do payload), o que garante que
  `zelo_orders_total_consistent` nunca pode ser violado por arredondamento
  entre a tolerância de 0.01 do normalizador JS e a aritmética exata do
  `numeric(14,2)` do Postgres — a verificação transacional afirma o número
  exato (40.00 subtotal / 6.50 delivery_fee / 2.00 discount / 44.50 total),
  não só a ausência de exceção.
- **Eventos informativos observados ao vivo (2026-09-16):**
  `IFOOD_INFORMATIONAL_EVENT_CODES = new Set(['DELIVERY_DROP_CODE_REQUESTED',
  'CANCELLATION_REQUESTED'])` e `isIfoodInformationalEventCode(event)` foram
  adicionados a `contracts.js` (aditivo; `IFOOD_EXTERNAL_STATUSES` e as
  demais siglas/aliases da Task 2 não foram alterados).
  `eventHandler.js` checa isso primeiro e retorna `{ outcome: 'processed' }`
  sem nenhuma chamada de rede ou de RPC.
- **`createIfoodIntegration.js`:** ganhou `getOrderDetail(orderId, { signal
  })`, passthrough fino para `adapter.getOrder`, sem retry/reinterpretação
  de erro própria (essa política vive no handler). `receiveEvent`,
  `connectMerchant`, `requestOrderAction`, `reconcileEvents` e
  `getConnectionHealth` não foram tocados.
- **`inboxProcessor.js` não foi modificado.** A janela de retry de detalhe
  usa `row.occurredAt` (já presente no shape retornado por
  `claim_ifood_events_v1`/repassado pelo processor desde a Task 7) em vez de
  um novo `receivedAt`: adicionar `receivedAt` exigiria alterar o retorno
  de `claim_ifood_events_v1`, que é parte da migration
  `20260916023512_ifood_mvp_foundation.sql` **já aplicada em produção** e
  que esta task foi instruída a nunca tocar. Essa é uma escolha
  deliberadamente mais simples que a alternativa aditiva sugerida pelo
  plano; documentada aqui em vez de arriscar alterar uma RPC de produção
  fora do escopo desta task.
- **`pessoa_id` fica sempre `null`** (Pessoas/clientes fora do MVP,
  conforme restrição global) e `product_id` de `zelo_order_items` fica
  sempre `null` (mapeamento de produto é a Task 12; item não mapeado não
  bloqueia o pedido nem movimenta estoque).
- RED: `npx vitest run tests/ifood.event-handler.test.js
  tests/ifood.canonical-projection-schema.test.js --reporter=verbose`
  falhou como esperado: `eventHandler.js` inexistente
  (`ERR_MODULE_NOT_FOUND`, 0 testes coletados) e, isoladamente, o schema
  test também falhava por falta do arquivo de migration antes do `supabase
  migration new`.
- GREEN focado: mesma suíte, **28/28** (17 do handler + 11 do schema/
  informational-code).
- GREEN combinado (regressão): `npx vitest run
  tests/ifood.event-handler.test.js tests/ifood.canonical-projection-schema.test.js
  tests/ifood.domain.test.js tests/ifood.inbox-processor.test.js
  tests/ifood.worker-runtime.test.js tests/ifood.contract-fixtures.test.js
  tests/ifood.persistence-schema.test.js tests/ifood.webhook-enqueue-schema.test.js
  tests/ifood.order-normalizer.test.js tests/onlineOrders.test.js
  --reporter=dot` → **10 arquivos, 119/119** (nenhum teste pré-existente
  alterado).
- `npm run check` → `5874 FILES 0 ERRORS 0 WARNINGS`. `npm run
  verify:migrations` → `107/107` baseline, `59/59` remoto, `58 forward`
  (a nova migration está contabilizada, nada foi aplicado ao banco
  vinculado). `git diff --check` (via `git add -N` dos 7 arquivos
  tocados/novos, depois `git reset`) → limpo. Varredura de LF via `node`
  (byte `13`/CR) nos 7 arquivos tocados/novos, incluindo os dois `.sql`
  novos → nenhum `\r` encontrado.
- **Harness descartável local (Docker Desktop disponível nesta sessão):**
  `powershell -ExecutionPolicy Bypass -File
  scripts/verify-supabase-baseline.ps1 -ApplyForwardMigrations
  -ExcludeTenantDataSeeds -PostMigrationVerification
  supabase/verification/ifood_mvp_foundation.sql,supabase/verification/ifood_webhook_enqueue.sql,supabase/verification/ifood_canonical_order_projection.sql`
  → **exit code 0**, `BASELINE_VERIFIED cutoff=20260813091000`,
  `post-migration verifiers passed: 3`. A primeira tentativa falhou por dois
  bugs reais pegos pela própria verificação transacional (não pela suíte
  Vitest, que só afirma forma do SQL): (1) `zelo_orders.revision` tem
  `default 1`, não `0` — a asserção "duplicate não bumpa revisão" estava
  comparando contra `0`; corrigido para `1`/`2` nas três asserções
  afetadas. (2) `revision`/`outcome`/`zelo_order_id` são também os nomes
  dos parâmetros `OUT` da função, então `update ... set revision = revision
  + 1` era ambíguo entre o parâmetro e a coluna (erro real do Postgres,
  `column reference "revision" is ambiguous`); corrigido qualificando a
  tabela (`update public.zelo_orders as zo set revision = zo.revision + 1,
  ...`). Isso é exatamente o tipo de bug que a task pediu para pegar antes
  de qualquer aplicação real — nenhuma migration foi tocada além da própria
  `ifood_canonical_order_projection`, ainda não aplicada em lugar nenhum.
  Nenhum `git stash` foi usado nesta task.
- **Não executado:** nenhuma migration foi aplicada ao Supabase vinculado;
  nenhuma chamada real ao iFood; nenhum repositório Supabase real foi
  escrito ou ligado a `workers/ifood/index.js`; `npm test` integral não foi
  executado (fora do escopo pedido — apenas os arquivos iFood-adjacentes e
  os vizinhos de regressão citados acima foram rodados, proporcional aos
  arquivos alterados).

**Revisão corretiva do coordenador (2026-09-16, antes do commit):**

- **Divergência de plataforma pré-existente encontrada, não relacionada ao
  iFood:** o coordenador reexecutou o harness de forma independente e o
  passo de comparação de plataforma falhou com `Platform-state mismatch:
  storage_policies` — reproduzido duas vezes de forma consistente. Diff
  direto contra o Supabase vinculado confirmou que a policy `zelochat-media
  public read` (schema `storage.objects`) existia no baseline
  (`20260813091000`) e não existe mais em produção hoje. É drift real,
  anterior a esta sessão e a esta task; não foi causado por nenhuma
  migration do iFood. Para ainda provar a Task 8 fim a fim, o coordenador
  editou uma cópia temporária de `scripts/verify-supabase-baseline.ps1`
  (transformando o `throw` daquele um passo específico em aviso), rodou o
  harness com essa cópia, e reverteu o arquivo rastreado com `git checkout`
  imediatamente depois (`git status`/`git diff` confirmam zero alteração
  residual no script). Com esse desvio pontual, todo o resto do harness
  passou de verdade: dump normalizado do schema/security bateu com produção
  (mesmo hash `5ecce9dd...`), e os 3 arquivos de verificação passaram
  (`post-migration verifiers passed: 3`), incluindo as asserções aritméticas
  e de isolamento de tenant da nova RPC. O script rastreado não foi alterado
  no commit desta task; a divergência de `storage_policies` fica registrada
  aqui como um achado separado para o dono decidir (atualizar o baseline ou
  investigar por que a policy foi removida).
- **Correção de integridade em `eventHandler.js`:** o detalhe do pedido
  buscado via `getOrderDetail` não era conferido contra o `externalOrderId`/
  `merchantId` esperados antes de normalizar e projetar — um retorno
  malformado ou trocado da API faria o handler gravar o pedido errado sob a
  identidade certa em `order_refs`. Adicionado um checkpoint defensivo que
  compara `normalizedOrder.externalOrderId`/`merchantId` contra a linha da
  inbox e retorna `terminal`/`ORDER_DETAIL_IDENTITY_MISMATCH` em caso de
  divergência, sem nunca chamar a RPC de projeção. Dois testes novos cobrem
  isso (`tests/ifood.event-handler.test.js`). Suíte iFood completa
  revalidada: `npx vitest run` nos 13 arquivos iFood-adjacentes → **190/190
  aprovados**. `npm run check` → 0 erros/0 avisos. `npm run verify:migrations`
  → 107/107 baseline, 59/59 remoto, 58 forward (inalterado).
- **Risco residual documentado, não corrigido nesta task:** o branch "não
  encontrado" da RPC (primeiro evento de um pedido novo) não trata
  `unique_violation` explicitamente como `create_zelo_order` faz. Duas
  chamadas concorrentes genuinamente simultâneas para o mesmíssimo
  `external_order_id` novo fariam a segunda falhar com exceção não tratada;
  como o worker dedicado é único por desenho do plano, essa corrida exigiria
  dois processos de worker rodando ao mesmo tempo, e mesmo se ocorrer o
  evento simplesmente volta para nova tentativa (`retryable` genérico) e o
  reprocessamento subsequente encontra a linha já criada — não há duplicação
  nem perda, apenas uma tentativa extra. Fica registrado para endurecer se
  necessário, sem exigir nova migration agora.

## Task 9: Reconciliar polling, ACK, presença e fail-closed

**Files:**
- Create: `src/lib/server/ifood/reconciliation.js`
- Create: `src/lib/server/ifood/connectionHealth.js`
- Create: `tests/ifood.reconciliation.test.js`
- Modify: `workers/ifood/runtime.js`
- Modify: `src/lib/server/ifood/adapters/httpIfoodAdapter.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** polling por merchant, persist-before-ACK, presença granular, health heartbeat e fail-closed padrão de 90s.

- [x] **Step 1: Escrever testes RED**

Cobrir evento de polling duplicado ao webhook, banco falhando antes do ACK, ACK parcial, merchant pausado, worker sem heartbeat, recuperação e filtro de até 1.000 merchants por lote.

- [x] **Step 2: Implementar reconciliação configurável**

O intervalo vem da conexão/config homologada na Task 1. Sandbox inicia conforme o contrato aprovado; não usar constante baseada em exemplo divergente da documentação.

- [x] **Step 3: Implementar fail-closed por merchant**

Com saúde vencida, desligar presença somente do merchant afetado, bloquear novos comandos e manter pedidos já persistidos visíveis. Recuperar somente com token, worker e configuração verdes.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/ifood.reconciliation.test.js tests/ifood.http-adapter.test.js`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: reconcile iFood events and presence`

**Resultado real (2026-09-16):**

- **Status: concluída no worktree, sem migration nova e sem tocar no Supabase
  vinculado.** `src/lib/server/ifood/reconciliation.js` implementa o seam
  `createIfoodReconciler({ adapter|integration, repository, clock, logger,
  batchSize, pollingIntervalMs })` e seu
  `runReconciliationCycle({ signal })`. O repositório fornece somente
  conexões não revogadas; `pending`, `active`, `degraded` e `paused` continuam
  sendo pollados para que eventos persistidos não sejam perdidos. `paused` e
  `degraded` afetam presença/comandos na política de saúde, não ingestão; o
  status `revoked` é filtrado antes da chamada ao provider.
- **Desvios:** `src/lib/server/ifood/adapters/httpIfoodAdapter.js` não precisou
  ser alterado porque já expunha `pollEvents({ merchantIds, signal })` e
  `ackEvents(ids, { signal })` com o limite de ACK do snapshot; não foi criado
  repositório Supabase real nem wiring novo no bootstrap fail-closed. A
  validação usou o launcher Node com `--configLoader runner` por causa da
  limitação de permissões do loader empacotado no Windows deste worktree.
- Cada chamada de polling é limitada a 1.000 merchants. Envelopes válidos
  passam por `enqueuePolledEvent(...)` antes do ACK; resultados `inserted` e
  `duplicate` são persistidos para fins de contagem e podem ser confirmados,
  enquanto `unknown_merchant` nunca é confirmado. Essa política deixa o
  evento redeliver quando houve corrida de revogação/conexão ou atribuição
  ainda não resolvida, permitindo deduplicação no próximo ciclo em vez de
  apagar a única chance de ingestão; merchants que nunca tiveram conexão
  continuam fora do backfill do MVP conforme G4. Falhas de persistência não
  entram na lista de ACK; falhas parciais de ACK são capturadas e os eventos
  persistidos voltam a ser deduplicados no ciclo seguinte.
- `recordPollSuccess(...)` é chamado por merchant somente depois de o polling
  daquele lote retornar com sucesso. O intervalo é recebido da configuração
  do reconciliador ou da conexão/repositório; não há fallback de cinco minutos
  copiado de exemplo documental. `normalizePollingIntervalMs(...)` rejeita
  valores não positivos e aplica clamp para o mínimo oficial de 30 segundos
  (`MIN_IFOOD_POLL_INTERVAL_MS`) quando a configuração é menor.
- `src/lib/server/ifood/connectionHealth.js` usa 90.000 ms como idade máxima
  configurável. A política mantém `ordersVisible: true`, bloqueia comandos e
  solicita `online: false` somente para o merchant afetado quando o worker,
  token, configuração ou status não estão verdes; a recuperação exige os
  três sinais verdes e pode solicitar `online: true` apenas após uma transição
  anterior para OFF. `setMerchantPresence({ merchantId, online, signal? })`
  é um seam injetado de repositório/adaptador: o adapter HTTP desta task não
  ganhou rota de escrita de presença e nenhum endpoint real foi inventado ou
  ligado.
- `workers/ifood/runtime.js` ganhou os hooks opcionais `reconcile` e
  `evaluateHealth`, ambos por ciclo e com `{ signal }`, usando o mesmo
  `reportError`/sanitização do hook `processInbox`. O bootstrap padrão em
  `workers/ifood/index.js` não fornece esses hooks e permanece fail-closed.
- RED literal: `npx vitest run tests/ifood.reconciliation.test.js` não
  iniciou porque o PowerShell bloqueia `npx.ps1`; o equivalente
  `node.exe node_modules/vitest/vitest.mjs run tests/ifood.reconciliation.test.js
  --configLoader runner` falhou antes da implementação por módulo ausente,
  com 0 testes coletados. GREEN focado pelo mesmo launcher: **1 arquivo,
  15/15**. GREEN combinado
  (`ifood.reconciliation`, `ifood.http-adapter`, `ifood.worker-runtime`,
  `ifood.inbox-processor`, `ifood.event-handler`, `api.ifood-webhook`) pelo
  mesmo comando: **6 arquivos, 123/123**.
- `npm.cmd run check` (equivalente ao `npm run check` bloqueado pelo mesmo
  PowerShell) terminou com **5.877 arquivos, 0 erros, 0 warnings**. O
  `git diff --check` do arquivo rastreado passou; os três arquivos novos foram
  verificados com `git diff --no-index --check` contra `NUL`, sem diagnóstico
  de whitespace. `git add -N`/`git reset` não pôde criar o `index.lock` do
  worktree por permissão do ambiente, sem alteração residual no índice. A
  varredura Node de byte 13 nos seis arquivos tocados/criados não encontrou
  CRLF.

**Revisão do coordenador (2026-09-16, antes do commit; implementação feita
pelo Codex CLI `gpt-5.6-luna`, effort `max`):**

- **Correção: token nunca era confirmado.** `connectionHealth.js` exige
  `last_token_at` fresco (≤ 90 s) para recuperar, mas nada no ciclo gravava
  essa evidência — uma loja ficaria em fail-closed para sempre. Um poll bem
  sucedido é uma chamada autenticada, então `recordPollSuccess` agora recebe
  `tokenConfirmedAt` (igual a `polledAt`) e o contrato do repositório exige
  gravar `last_poll_at` e `last_token_at`. Teste ajustado; suíte combinada
  (6 arquivos) → **123/123**.
- **Requisito para a task que ligar o worker real:** o intervalo padrão do
  worker é 300 s (Task 4), incompatível com o limite de saúde de 90 s — com
  esse padrão toda loja ficaria sempre "stale" e sem presença. Ao ligar
  `reconcile`/`evaluateHealth` no bootstrap, o intervalo precisa ficar entre
  30 s (mínimo do iFood) e bem abaixo de 90 s, e o heartbeat
  (`worker_heartbeat_at`) precisa ser gravado a cada ciclo. Não alterado agora
  porque nada está ligado ao bootstrap padrão.

## Task 10: Criar comandos assíncronos e APIs operacionais

**Files:**
- Create via CLI (arquivo já gerado pelo orquestrador): `supabase/migrations/20260916234658_ifood_order_commands.sql`
- Create: `supabase/verification/ifood_order_commands.sql`
- Create: `src/lib/server/ifood/commandService.js`
- Create: `src/lib/server/ifood/commandRepository.js`
- Create: `src/lib/server/ifood/cancellationReasonsService.js`
- Create: `src/lib/server/ifood/commandProcessor.js`
- Create: `src/routes/api/integrations/ifood/orders/[orderId]/commands/+server.js`
- Create: `src/routes/api/integrations/ifood/orders/[orderId]/cancellation-reasons/+server.js`
- Create: `tests/api.ifood-order-commands.test.js`
- Create: `tests/ifood.command-processor.test.js`
- Create: `tests/ifood.order-commands-schema.test.js`
- Modify: `workers/ifood/runtime.js`
- Modify: `src/lib/server/ifood/eventHandler.js`
- Modify: `tests/ifood.event-handler.test.js`, `tests/ifood.worker-runtime.test.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `queued → sending → accepted_http → confirmed_event` e falhas retryable/terminal/expired; confirmação, preparo, pronto, despacho e cancelamento dinâmico.

- [x] **Step 1: Escrever testes RED de autorização e idempotência**

Testar owner, subusuário com/sem a permissão alinhada ao PDV, empresa cruzada,
conexão degradada, revisão repetida e ação incompatível com modalidade/estado.

- [x] **Step 2: Implementar criação de intenção**

A rota valida sessão pelo `supabaseAdmin`, resolve owner por `getServerAccessContext`, confere a conexão e persiste a intenção. Não chama o iFood na requisição do browser.

- [x] **Step 3: Implementar envio no worker**

Mapear cada intent ao adapter da Task 5. `202` marca apenas `accepted_http`. Evento de resultado correlaciona e marca `confirmed_event`; timeout vira pendência, nunca transição comercial otimista.

- [x] **Step 4: Validar**

Run: `node.exe node_modules/vitest/vitest.mjs run tests/api.ifood-order-commands.test.js tests/ifood.command-processor.test.js tests/ifood.order-commands-schema.test.js tests/ifood.event-handler.test.js tests/ifood.worker-runtime.test.js tests/ifood.inbox-processor.test.js tests/ifood.reconciliation.test.js tests/ifood.http-adapter.test.js tests/api.ifood-webhook.test.js --configLoader runner --reporter=dot`
Run: `npm.cmd run check`
Run: `npm.cmd run verify:migrations`
Run: `git diff --check` e varredura Node de byte 13 nos arquivos tocados/criados.

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add asynchronous iFood order commands`

**Resultado real (2026-09-16):**

- **Status: Concluída (harness pendente do coordenador).** A migração
  `supabase/migrations/20260916234658_ifood_order_commands.sql` foi preservada
  no arquivo gerado pelo orquestrador; o SQL não foi aplicado a nenhum banco e
  não foi executado no harness nesta sessão. A migração não altera tabelas,
  constraints ou funções anteriores: cria somente quatro RPCs novas,
  SECURITY DEFINER, `set search_path = ''`, checagem de `service_role`,
  revogação de `public`/`anon`/`authenticated` e `grant execute` exclusivo para
  `service_role`.
- `enqueue_ifood_order_command_v1(uuid, uuid, text, integer, jsonb, text)`
  resolve `order_refs` por empresa e pedido, trava `zelo_orders`, exige revisão
  atual e conexão `active`, valida a transição e o payload de cancelamento e
  retorna `queued`, `duplicate`, `not_found`, `revision_conflict`,
  `connection_unavailable`, `invalid_transition` ou `invalid_payload`. A chave
  `(connection_id, external_order_id, intent, expected_external_revision)` e a
  `idempotency_key` existentes são tratadas explicitamente por
  `unique_violation`; nunca há segunda linha nem mudança otimista de
  `zelo_orders.status`.
- `confirm_ifood_order_commands_v1(text, text, text)` mapeia `CONFIRMED`,
  `PREPARATION_STARTED`, `READY_TO_PICKUP`, `DISPATCHED` e `CANCELLED` para os
  cinco intents e retorna a quantidade movida para `confirmed_event`, aceitando
  `accepted_http`, `sending`, `queued` e `failed_retryable` como estados ainda
  correlacionáveis. Status externo desconhecido retorna `0`.
  `expire_ifood_accepted_commands_v1(integer)` retorna a quantidade movida de
  `accepted_http` para `expired`, exige limiar entre 60 e 86400 segundos e não
  toca o pedido canônico. `get_ifood_order_ref_v1(uuid, uuid)` retorna
  `(merchant_id, external_order_id, connection_status)` dentro da empresa e
  não retorna linha cross-company.
- A matriz escolhida é: `confirm` somente de `pending_review`;
  `start_preparation` de `accepted`; `ready_to_pickup` de `accepted` ou
  `preparing` somente quando `fulfillment.deliveredBy` não é `IFOOD`;
  `dispatch` de `preparing` ou `ready` somente em `fulfillment.type = delivery`
  com `deliveredBy = MERCHANT`;
  `cancel` de qualquer estado não terminal (`delivered`, `rejected` e
  `cancelled` são terminais). O mapeamento de permissões é deliberadamente o
  mesmo de `transition_zelo_order`: `confirm`/`dispatch` exigem
  `pedidos.acessar`, `start_preparation`/`ready_to_pickup` exigem
  `pedidos.cozinha` e `cancel` exige `pedidos.cancelar`; `pedidos.receber` não
  é usado aqui porque o RPC do PDV reserva essa chave para `deliver`, não para
  `dispatch`.
- `POST /api/integrations/ifood/orders/:orderId/commands` recebe
  `{ intent, expectedRevision, cancellationCode?, reason? }`, resolve a
  `empresa_perfil.id` pelo owner efetivo no servidor e retorna `202`
  `{ commandId, status: 'queued' }` para `queued`, `200` com o status existente
  para `duplicate`, `404` para `order_not_found`, `409` para
  `revision_conflict`/`connection_unavailable` e `422` para
  `invalid_transition`/`invalid_payload`. A chave de idempotência é derivada
  de empresa, pedido, intent e revisão; qualquer chave do cliente é ignorada.
  `GET /api/integrations/ifood/orders/:orderId/cancellation-reasons` exige
  `pedidos.cancelar`, conexão ativa e as credenciais `$env`, chama o adapter
  somente no servidor e expõe apenas `[{ code, description }]`; ausência de
  segredo retorna `503` e erro do provider retorna `502` genérico. Todas as
  respostas usam `Cache-Control: no-store` e não expõem SQL, payload, token ou
  PII.
- `createIfoodCommandProcessor` reivindica leases sequencialmente, chama
  `confirm`, `startPreparation`, `readyToPickup`, `dispatch` ou
  `requestCancellation` com `external_order_id`, grava `202`/`accepted: true`
  como `{ status: 'accepted_http' }` sanitizado, retryable como
  `failed_retryable` com `nextAttemptAt` e cliente não retryable como
  `failed_terminal`. A política de retry registra o trade-off de double-send:
  a chave por intent/revisão torna a repetição esperada como no-op de transição,
  mas o snapshot não prova idempotência do provider; por isso o adapter não
  repete POST ambíguo automaticamente e o worker registra o reenvio bounded.
  Merchant não saudável termina como `failed_retryable` com
  `CONNECTION_UNHEALTHY` sem chamar o adapter. O sweep usa 600 segundos (10
  minutos) por padrão.
- Após projeção `applied` ou `ignored_duplicate`, `eventHandler` chama o seam
  opcional `confirmCommandsForEvent({ merchantId, externalOrderId,
  externalStatus, signal })`. Se a correlação falhar, o evento continua
  `processed` e uma linha genérica é registrada, pois a projeção já foi
  commitada; outro evento ou reconciliação pode refazer a correlação. O runtime
  recebeu o hook opcional `processCommands` no mesmo padrão de `processInbox`,
  `reconcile` e `evaluateHealth`, sem alterar o bootstrap fail-closed.
- **Desvio explícito da lista inicial de arquivos:** além dos arquivos
  inicialmente listados, foram necessários a migração nova com o timestamp
  fornecido pelo orquestrador, a verificação transacional SQL, o schema test,
  `commandRepository.js` e `cancellationReasonsService.js`; também houve a
  mudança mínima de `eventHandler.js` e os testes de correlação/runtime.
- RED: `node.exe node_modules/vitest/vitest.mjs run
  tests/api.ifood-order-commands.test.js tests/ifood.command-processor.test.js
  tests/ifood.order-commands-schema.test.js --configLoader runner
  --reporter=verbose` → **3 suítes falharam, 0 testes coletados**, por dois
  módulos ausentes e `supabase/verification/ifood_order_commands.sql` ausente.
  GREEN: o launcher equivalente executando os nove arquivos listados acima →
  **9 arquivos, 184/184 testes aprovados**. `npm.cmd run check` → **5.888
  arquivos, 0 erros, 0 warnings**. `npm.cmd run verify:migrations` →
  **107/107 artefatos baseline, 59/59 versões remotas, 59 migrations forward**.
  `git diff --check` ficou limpo para os arquivos versionados e os 11 novos;
  a varredura Node de byte 13 cobriu 17 arquivos e encontrou `bad=0`. O
  harness descartável e qualquer aplicação da migração permanecem
  deliberadamente não executados.

**Revisão do coordenador (2026-09-16, antes do commit; implementação pelo
Codex CLI `gpt-5.6-luna`, effort `max`, em duas execuções — a primeira parou
corretamente porque o Supabase CLI não podia gravar telemetria na sandbox, e
o coordenador gerou o arquivo da migration pelo CLI oficial):**

- **Bug corrigido: motivo do cancelamento era descartado.** Em
  `enqueue_ifood_order_command_v1` o payload era reconstruído só com
  `cancellationCode` e depois lia `reason` do objeto já reconstruído, então
  o motivo nunca era gravado. Agora os dois campos são lidos do payload
  original. Nova verificação (7b) prova que só `cancellationCode`/`reason`
  (aparados) são persistidos e campos extras do browser são descartados.
- **Correção de UX: comando que falhou nunca podia ser reenviado.** A chave
  única é `(conexão, pedido, intent, revisão esperada)` e a revisão comercial
  só muda com evento do iFood; um `confirm` em `failed_terminal`/`expired`
  bloqueava o operador para sempre com `duplicate`. Agora esses dois estados
  finais de falha são re-enfileirados na mesma linha (`outcome = 'requeued'`,
  HTTP 202, tentativas/erros zerados); comandos vivos ou confirmados seguem
  `duplicate`. Verificação 5b prova uma única linha resetada; teste de API
  cobre o mapeamento.
- **Verificação SQL do Codex nunca tinha rodado:** falhava no fixture
  (`raw_app_meta_data` recebia `text` por causa do `UNION ALL`). Corrigido
  com `'{}'::jsonb`.
- **Harness descartável:** com o mesmo desvio pontual e revertido da
  divergência pré-existente de `storage_policies` (script rastreado inalterado,
  confirmado por `git diff`), `BASELINE_VERIFIED` com o mesmo hash
  `5ecce9dd...` e **`post-migration verifiers passed: 4`** (fundação, webhook,
  projeção e comandos).
- Suítes iFood (12 arquivos) → **220/220**; `npm run check` → 0/0;
  `npm run verify:migrations` → 107/107, 59/59, 59 forward.

## Task 11: Mostrar iFood nas filas de Pedidos e Cozinha

**Files:**
- Create: `src/lib/components/orders/OrderSourceBadge.svelte`
- Create: `src/lib/components/orders/IfoodSyncState.svelte`
- Create: `src/lib/orders/ifoodPresentation.js`
- Create: `tests/ifood.order-presentation.test.js`
- Create: `tests/ifood.queue-ui.test.js`
- Modify: `src/lib/onlineOrders.js`
- Modify: `src/routes/app/pedidos/+page.svelte`
- Modify: `src/routes/app/pedidos/cozinha/+page.svelte`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** origem real, badge textual, sincronização separada do status, SLA 4/6/8 minutos, códigos e seção de agendados.

- [x] **Step 1: Escrever testes RED**

Provar que `mapCanonicalOrder` usa `row.source` em vez do hardcode `zelomenu`; que ações iFood chamam a API da Task 10; que outros canais continuam usando `transition_zelo_order`; e que agendado não entra cedo na Cozinha.

- [x] **Step 2: Implementar apresentação pura**

`ifoodPresentation.js` calcula badge, relógio, severidade, comando pendente, `preparationStartAt` e contingência do Portal sem conhecer contratos HTTP externos.

- [x] **Step 3: Integrar as telas existentes**

Manter `zelo_orders` como única fonte. Exibir alerta visual persistente, item não mapeado e ações permitidas. Não criar fila paralela.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/ifood.order-presentation.test.js tests/ifood.queue-ui.test.js tests/onlineOrders.test.js tests/orderPresentation.test.js`

Run: `npm run check`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: show iFood orders in operational queues`

**Resultado real (2026-09-16):**

- Primeira task com a divisão de execução: backend pelo Codex CLI
  (`gpt-5.6-luna`, effort max) em duas partes, frontend pelo Claude, revisão
  e integração pelo coordenador.
- Backend parte 1: `get_ifood_order_sync_state_v1(uuid, uuid[])`
  (`20260917004212_ifood_order_sync_state.sql`, service-role only,
  tenant-scoped, comando vivo primeiro, `TOO_MANY_ORDER_IDS` acima de 200),
  `syncStateService.js` (1..100 UUIDs; owner ou subusuário com
  `pedidos.acessar`/`pedidos.cozinha`) e
  `GET /api/integrations/ifood/orders/sync-state` com `no-store`.
- Backend parte 2 (desvio necessário, bugs encontrados no teste real do
  detalhe do pedido): o normalizer lia `delivery.address`, mas o iFood envia
  `delivery.deliveryAddress` — o endereço se perdia; e copiava o `customer`
  inteiro, gravando CPF (`documentNumber`), `segmentation` e
  `ordersCountOnMerchant` em `zelo_orders.customer`. Agora é allow-list
  (nome, telefone com localizador, endereço sem coordenadas) e o normalizer
  expõe `displayId`. A migration
  `20260917005456_ifood_projection_display_fields.sql` recria
  `project_ifood_order_event_v1` só para gravar `fulfillment.ifood`
  (displayId, externalOrderId, preparationStartAt, scheduled, janela,
  pickupCode, deliveryCode) com nulls explícitos.
- Frontend: `ifoodPresentation.js` (badge, SLA 4/6/8, agenda, intenção
  espelhando a matriz SQL da Task 10, apresentação de sync, roteamento
  comando × transição), `ifoodCommandsClient.js` (POST de comando com
  `expectedRevision`, motivos de cancelamento, sync-state), componentes
  `OrderSourceBadge`/`IfoodSyncState`, `mapCanonicalOrder` usando
  `row.source` e o bloco iFood; Pedidos com ação iFood, códigos, itens sem
  vínculo e modal de motivos; Cozinha com faixa de agendados e ações por
  comando. Pedido iFood nunca é fechado como venda nesta task.
- Correção da revisão: um comando `failed_terminal`/`expired` já resolvido no
  Portal do Parceiro ficaria exibido para sempre; agora some quando o status do
  pedido já satisfaz a intenção (problemas de conexão nunca são ocultados).
- Desvio do verificador: `ifood_projection_display_fields.sql` usava
  `foreach ... in array[...]` (erro de sintaxe no harness); corrigido para
  `in array array[...]`.
- Validação: suíte iFood + filas 23 arquivos/316 testes; `npm run check` 0
  erros/0 warnings; `npm run verify:migrations` 107/107 baseline, 59/59
  remotas, 61 forward; harness descartável com
  `-ApplyForwardMigrations -ExcludeTenantDataSeeds` e os 6 verificadores iFood
  (foundation, webhook, projeção, comandos, sync-state, display fields):
  `post-migration verifiers passed: 6`. O throw de `storage_policies` (drift
  pré-existente de produção, sem relação com iFood) foi rebaixado a aviso só
  localmente durante a execução e revertido; não foi commitado.


## Task 12: Mapear produtos progressivamente e controlar estoque

**Files:**
- Create via CLI: `supabase/migrations/*_ifood_product_mapping_stock.sql`
- Create: `supabase/verification/ifood_product_mapping_stock.sql`
- Create: `src/lib/server/ifood/productMappingService.js`
- Create: `src/routes/api/integrations/ifood/product-mappings/+server.js`
- Create: `tests/api.ifood-product-mappings.test.js`
- Create: `tests/ifood.product-mapping-stock-schema.test.js`
- Modify: `src/lib/server/ifood/eventHandler.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** sugestão exata por `externalCode`, vínculo manual, compromisso no `CONFIRMED` e restauração única no cancelamento.

- [x] **Step 1: Escrever testes RED**

Cobrir match exato, nome semelhante apenas sugerido, item sem mapping, mapping cruzado, duplo `CONFIRMED`, cancelamento duplicado e cancelamento após compromisso.

- [x] **Step 2: Implementar migration e serviço**

Usar ledger de `stock_commitments` com unique por order/item/produto e eventos `committed|released`. O saldo muda e o ledger é gravado na mesma transação.

- [x] **Step 3: Validar**

Run: `npx vitest run tests/api.ifood-product-mappings.test.js tests/ifood.product-mapping-stock-schema.test.js`

Run: `powershell -ExecutionPolicy Bypass -File scripts/verify-supabase-baseline.ps1 -VerificationFile supabase/verification/ifood_product_mapping_stock.sql`

- [x] **Step 4: Atualizar tracker e commitar**

Commit: `feat: add progressive iFood product mapping`

**Resultado real:** 2026-09-17 (Cursor Cloud, branch `cursor/ifood-task-12-cdb9` a partir de `codex/ifood-mvp` @ `aa9297d`).

- Migration CLI `20260917014734_ifood_product_mapping_stock.sql` (arquivo gerado por `npx supabase migration new`; sem alterar as tabelas foundation já aplicadas). RPCs service-role-only:
  - `suggest_ifood_product_mapping_v1` — match exato `externalCode === produtos.id::text` upserta no máximo `suggested`; nomes semelhantes retornam só leitura (`reason=similar_name`), nunca auto-confirmam.
  - `confirm_ifood_product_mapping_v1` — vínculo manual `confirm`/`disable` tenant-scoped; produto de outra empresa → `product_not_found` sem gravar.
  - `commit_ifood_stock_for_event_v1` — no `CONFIRMED`, compromete estoque só para mapping `confirmed`; ledger unique `(order_ref_id, external_item_id, product_id)`; saldo + ledger na mesma TX; item sem mapping não move estoque; estoque insuficiente pula o item (não bloqueia o pedido já projetado); atualiza `zelo_order_items.product_id` e `zelo_orders.stock_committed_at`.
  - `release_ifood_stock_for_event_v1` — no `CANCELLED`, restaura só compromissos ainda `committed`; segundo cancel é no-op (`duplicate_count`).
- API `GET/POST /api/integrations/ifood/product-mappings` + `productMappingService.js` / `productMappingRepository.js`. Auth bearer + `getServerAccessContext`; leitura exige owner ou `produtos.visualizar`/`gerenciar`; confirmação exige owner ou `produtos.gerenciar`. Nunca vaza texto SQL.
- `eventHandler.js`: após projeção `applied`/`ignored_duplicate`, hooks opcionais `commitStockForEvent` (CONFIRMED) e `releaseStockForEvent` (CANCELLED), best-effort como a correlação de comandos — falha não reabre a inbox.
- **Arquivo extra vs lista do plano:** `productMappingRepository.js` (mesmo padrão da Task 10) e testes de stock no `ifood.event-handler.test.js`.
- GREEN: `npx vitest run tests/api.ifood-product-mappings.test.js tests/ifood.product-mapping-stock-schema.test.js tests/ifood.event-handler.test.js` → **37/37**. Suíte iFood combinada (`tests/ifood*.test.js` + `tests/api.ifood*.test.js` + `tests/onlineOrders.test.js`) → **24 arquivos / 328 testes**. `npm run verify:migrations` → **107/107 baseline, 59/59 remote, 62 forward**. Varredura LF (byte 13) nos arquivos tocados → limpo.
- **Harness descartável:** não executado neste ambiente Linux Cloud (sem Docker/`pwsh`). A verificação SQL transacional está versionada em `supabase/verification/ifood_product_mapping_stock.sql` e coberta pelo schema test; o coordenador deve rodar o harness na máquina com o bypass temporário de `storage_policies` documentado em `CURRENT.md` antes de aplicar a migration em produção.
- Migration **não aplicada** ao Supabase vinculado nesta sessão (exige autorização explícita do dono).

## Task 13: Coordenar impressão sem duplicidade

**Files:**
- Create: `src/lib/orders/ifoodPrinting.js`
- Create: `tests/ifood.printing.test.js`
- Modify: `src/lib/orderAutoPrint.js`
- Modify: `src/lib/orderPrint.js`
- Modify: `src/routes/app/pedidos/+page.svelte`
- Modify: `docs/superpowers/specs/2026-09-05-browser-print-station-design.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** política `print_owner='zelo'|'external'`, impressão no horário de preparo e dedupe pelo pedido canônico.

- [x] **Step 1: Escrever testes RED**

Cobrir Zelo, externo, conexão sem configuração, imediato, agendado antes/depois do preparo, realtime+poll concorrentes e `PRINT_OUTCOME_UNKNOWN`.

- [x] **Step 2: Implementar o filtro antes de enfileirar**

Pedidos iFood imprimem apenas se a conexão escolher Zelo e o horário de preparo chegou. Dedupe existente continua usando `zelo_orders.id`; falha incerta nunca repete automaticamente.

- [x] **Step 3: Validar**

Run: `npx vitest run tests/ifood.printing.test.js tests/orderAutoPrint.test.js tests/orderPrint.test.js tests/printService.reliability.test.js`

- [x] **Step 4: Atualizar tracker e commitar**

Commit: `feat: coordinate iFood order printing`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch `cursor/ifood-task-12-cdb9` após Task 12).

- `src/lib/orders/ifoodPrinting.js`: política pura `shouldPrintIfoodOrder` /
  `normalizePrintOwner` com ações `print|skip|defer`. `external` e config
  inválida/ausente → skip; agendado antes de `preparationStartAt` → defer
  (reusa `ifoodScheduleState`); não-iFood → `not_ifood` (caminho legado).
- `orderAutoPrint.selectOrdersToAutoPrint` ganhou hook opcional `shouldEnqueue`.
- `canonicalOrderAutoPrintRuntime` aplica a política, mantém `deferredIds`
  sem reservar até o horário de preparo (também para pedidos fora da janela
  de `criado_em`), e preserva a regra de `PRINT_OUTCOME_UNKNOWN` (não
  `release`, sem auto-retry). Auto-print real continua global em
  `CanonicalOrderAutoPrinter` (não na página de Pedidos).
- `orderPrint.buildOrderText` mostra `IFOOD #displayId` e códigos de
  coleta/entrega quando `source='ifood'`.
- `/app/pedidos` só documenta que a reimpressão manual permanece escape hatch
  fora de `print_owner`.
- **Desvio / pendência:** `ifood_internal.connections.print_owner` ainda não
  tem leitura browser (schema privado; wizard é Task 17/18). O printer injeta
  `resolvePrintOwner` com fallback `'zelo'` (default da foundation) e ainda
  honra `order.ifood.printOwner` quando presente. Política fail-closed para
  `null`/inválido está coberta nos testes.
- GREEN: `npx vitest run tests/ifood.printing.test.js tests/orderAutoPrint.test.js
  tests/orderPrint.test.js tests/printService.reliability.test.js
  tests/canonicalOrderAutoPrintRuntime.test.js` → **5 arquivos / 23 testes**.
  Spec da estação de impressão atualizada com a seção iFood.

## Task 14: Criar venda operacional e estorno auditável

**Files:**
- Create via CLI: `supabase/migrations/*_ifood_sales_and_reversals.sql`
- Create: `supabase/verification/ifood_sales_and_reversals.sql`
- Create: `tests/ifood.sales-schema.test.js`
- Modify: `src/lib/server/ifood/eventHandler.js`
- Modify: `docs/data/SCHEMA_RLS.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `vendas.canal_origem`, índice por owner/data/canal, `vendas_estornos`, `materialize_ifood_sale_v1` e `reverse_ifood_sale_v1` service-role-only.

- [x] **Step 1: Escrever testes RED financeiros**

Provar: somente `CONCLUDED`; uma venda por pedido; `id_caixa is null`; sem `pessoas`; itens/complementos/desconto/frete/pagamento preservados; dinheiro não afeta caixa; evento duplicado não duplica; cancelamento inequívoco cria um estorno sem apagar venda; parcial/ambíguo cria pendência.

- [x] **Step 2: Implementar funções atômicas**

O canal padrão de vendas antigas deve ser `pdv`, mas o backfill primeiro recupera `zelo_orders.source` pelo `sale_id`; somente vendas sem referência canônica recebem `pdv`. A constraint conhecida aceita `pdv`, `zelomenu`, `zelochat`, `mesa`, `manual` e `ifood`. A migration também substitui `ensure_zelo_order_sale`: para `source='ifood'`, a função exige estado concluído, força `id_caixa = null`, não cria fiado/Pessoa e delega ao contrato idempotente da integração. Não estimar comissão nem preencher `vendas_taxas_plataforma`.

- [x] **Step 3: Executar verificação SQL com duas empresas**

Run: `npx vitest run tests/ifood.sales-schema.test.js tests/canonicalOrderSales.test.js tests/salesCreationRbacSchema.test.js`

Run: `powershell -ExecutionPolicy Bypass -File scripts/verify-supabase-baseline.ps1 -VerificationFile supabase/verification/ifood_sales_and_reversals.sql`

- [x] **Step 4: Atualizar tracker e commitar**

Commit: `feat: materialize iFood sales and reversals`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch `cursor/ifood-task-12-cdb9` após Task 13).

- Migration `20260917020813_ifood_sales_and_reversals.sql` (gerada por
  `npx supabase migration new`, wrapped em `begin;`/`commit;`, LF only):
  - `public.vendas` ganha `canal_origem text not null` com `check` em
    `pdv|zelomenu|zelochat|mesa|manual|ifood` e índice
    `idx_vendas_usuario_created_canal (id_usuario, created_at, canal_origem)`.
    Backfill via `zelo_orders.sale_id` (mapeando `whatsapp`/
    `legacy_zelochat → zelochat`, `legacy_pedido → pdv`); toda venda sem
    vínculo canônico recebe `pdv` — simplificação deliberada só para
    histórico, documentada em `SCHEMA_RLS.md`. Trigger novo
    `vendas_default_canal_origem` (BEFORE INSERT, sem grants especiais)
    estampa `mesa`/`pdv` quando o INSERT não informa `canal_origem`
    explicitamente.
  - `public.vendas_estornos`: `event_id unique`, `status in
    ('applied','pending_review')`, índice único parcial
    `idx_vendas_estornos_applied_per_venda (id_venda) where status =
    'applied'` (no máximo um estorno `applied` por venda). RLS: SELECT
    exige owner + `relatorios.ver` (mesmo padrão de
    `vendas_taxas_plataforma`); INSERT/UPDATE/DELETE só `service_role`.
  - `materialize_ifood_sale_v1(merchant_id, external_order_id)` e
    `reverse_ifood_sale_v1(merchant_id, external_order_id, event_id)`:
    `SECURITY DEFINER`, `search_path = ''`,
    `coalesce(current_setting('role', true) = 'service_role', false)` —
    endurecimento copiado de `20260917014734_ifood_product_mapping_stock.sql`.
    Sem `execute` para `anon`/`authenticated`.
  - `materialize_ifood_sale_v1` só age quando `source='ifood'` e
    `status='delivered'`; `id_caixa` sempre `null`; nunca toca
    `pessoas`/`fiado_lancamentos`/`vendas_taxas_plataforma`;
    `client_sale_id = 'zelo-order:'||id` garante idempotência
    (`already_exists` na segunda chamada). Itens e `modifiers` copiados de
    `zelo_order_items`; cada `payment.methods[]` gera sua própria linha em
    `vendas_pagamentos`, com `fiado` sempre coagido para `outro` (CASE
    copiado de `ensure_zelo_order_sale` no baseline).
  - `reverse_ifood_sale_v1` nunca apaga a venda. `event_id` repetido, ou
    segundo `event_id` distinto para venda já revertida, retorna
    `duplicate`. Pedido nunca entregue retorna `no_sale`. Cancelamento
    inequívoco (`status='cancelled'` e total do pedido ainda igual ao
    `valor_total` gravado) grava `status='applied'`. Qualquer ambiguidade
    (pedido não cancelado, ou total divergente porque um evento terminal
    posterior sobrescreveu o snapshot) grava `status='pending_review'`.
  - `ensure_zelo_order_sale` (`create or replace`): novo branch para
    `source='ifood'` exige `delivered`, resolve
    `merchant_id`/`external_order_id` via `ifood_internal.order_refs` e
    delega a `materialize_ifood_sale_v1`. Esse branch é só
    reparo/consistência — o trigger `zelo_order_sale_on_deliver` dispara
    **antes** do próprio UPDATE aplicar `status`/`total`/`payment`
    (semântica BEFORE ROW do Postgres), então nunca materializa nada no
    caminho automático; a materialização real do fluxo ao vivo é a chamada
    explícita e best-effort do `eventHandler.js` depois da transação de
    projeção comitar. Para as demais fontes, o corpo genérico ganhou o
    cálculo de `canal_origem` a partir de `source` antes do INSERT.
- `src/lib/server/ifood/salesRepository.js` (novo, mesmo padrão de
  `productMappingRepository.js`): `materializeSaleForEvent` e
  `reverseSaleForEvent` chamando as duas RPCs via `supabase.rpc(...).single()`.
- `eventHandler.js`: após projeção `applied`/`ignored_duplicate`, hook
  best-effort `materializeSaleForEvent` em `CONCLUDED` e
  `reverseSaleForEvent` em `CANCELLED` — mesmo padrão dos hooks de estoque
  da Task 12; falha não reabre a inbox.
- **Arquivo extra vs lista do plano:** `salesRepository.js` (repositório
  dedicado em vez de estender `productMappingRepository.js`, para não
  misturar domínio de mapeamento de produto com domínio financeiro).
- GREEN: `npx vitest run tests/ifood.sales-schema.test.js
  tests/ifood.event-handler.test.js tests/canonicalOrderSales.test.js
  tests/salesCreationRbacSchema.test.js` → **4 arquivos / 42 testes**.
  `canonicalOrderSales.test.js` e `salesCreationRbacSchema.test.js` continuam
  verdes sem alteração — eles leem `.ai/migrations/canonical_order_sales_
  2026_07_23.sql`, arquivo legado não tocado por esta task.
- **Harness descartável:** não executado neste ambiente Linux Cloud (sem
  Docker/`pwsh`). A verificação SQL transacional multi-tenant está
  versionada em `supabase/verification/ifood_sales_and_reversals.sql`
  (fixtures de duas empresas, happy path completo, idempotência de
  materialize/reverse, estorno ambíguo por total divergente, cancelamento
  pré-entrega) e coberta pelo schema test; o coordenador deve rodar o
  harness na máquina com o bypass temporário de `storage_policies`
  documentado em `CURRENT.md` antes de aplicar a migration em produção.
- Migration **não aplicada** ao Supabase vinculado nesta sessão (exige
  autorização explícita do dono).

## Task 15: Expor vendas iFood nos relatórios existentes

**Files:**
- Create: `src/lib/finance/salesChannel.js`
- Create: `tests/finance.sales-channel.test.js`
- Modify: `src/routes/relatorios/+page.svelte`
- Modify: `tests/relatoriosLayout.test.js`
- Modify: `tests/reportExports.test.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** filtro `Todos|PDV|ZeloMenu|ZeloChat|Mesas|Manual|iFood`, cards comparativos, badge na venda e estornos/cancelamentos.

- [x] **Step 1: Escrever testes RED**

Criar fixtures com múltiplos canais e provar quantidade, bruto, ticket médio, pagamento, exportação e estorno. Comissão/líquido iFood deve aparecer como `Indisponível`, não zero.

- [x] **Step 2: Atualizar todas as queries de venda**

Adicionar `canal_origem` às seleções de caixa e período e `vendas_estornos` em lotes. Centralizar labels/cores em `salesChannel.js`; usar variáveis CSS existentes.

- [x] **Step 3: Validar**

Run: `npx vitest run tests/finance.sales-channel.test.js tests/relatoriosLayout.test.js tests/reportExports.test.js tests/finance.reportPaymentPresentation.test.js`

Run: `npm run check`

- [x] **Step 4: Atualizar tracker e commitar**

Commit: `feat: report sales by origin channel`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch `cursor/ifood-task-12-cdb9` após Task 14).

- `src/lib/finance/salesChannel.js` (novo): fonte única de verdade para o
  canal de venda — `SALES_CHANNELS` (id/label/cor Tailwind/hex, um por
  valor do `CHECK` de `vendas.canal_origem`: `pdv`, `zelomenu`, `zelochat`,
  `mesa`, `manual`, `ifood`), `SALES_CHANNEL_FILTER_OPTIONS` (`Todos` +
  os seis canais, na ordem exata do filtro pedido),
  `normalizeSalesChannel` (fallback para `pdv`, mesmo default do trigger
  `vendas_default_canal_origem` da Task 14), `getChannelVisual`/
  `getChannelLabel` (badge), `channelHasCommissionData` +
  `CHANNELS_WITHOUT_COMMISSION_DATA` (hoje só `ifood`, porque
  `materialize_ifood_sale_v1` nunca grava `vendas_taxas_plataforma`),
  `COMMISSION_UNAVAILABLE_LABEL = 'Indisponível'`,
  `buildVendaChannelMap`/`filterVendasByChannel` (join `id_venda` → canal),
  `summarizeSalesByChannel` (card comparativo: qtd/bruto/ticket médio/
  comissão/líquido/estornos aplicados por canal — comissão e líquido ficam
  `null`, nunca `0`, para canais sem dado de taxa) e `summarizeEstornos`
  (agrega `vendas_estornos`, com `pending_review` contado separadamente e
  nunca somado ao valor estornado).
- `src/routes/relatorios/+page.svelte`: `canal_origem` adicionado ao
  `select` de `vendas` nos dois modos (caixa e período — inclusive no loop
  paginado de período). Novo helper `carregarEstornosPorVendas` (mesmo
  padrão de `carregarComandasMesaPorVendas`: `chunkArray(vendaIds, 1000)` +
  `Promise.all`) carrega `vendas_estornos` — chamado direto no modo caixa e
  encaixado no array `promises` do modo período (mesmo padrão de
  `vendas_taxas_plataforma`, com placeholder quando não há `vendaIds`).
  Dois novos selects nativos (`id="select-canal-caixa"` /
  `id="select-canal-periodo"`, mesma classe `input-form` dos selects
  legados já existentes no arquivo — hotspot, não retrofitar para shadcn)
  alimentam `canalFiltroCaixa`/`canalFiltroPeriodo`. Cada modo ganha duas
  seções novas (`card-mini`, vocabulário de card já existente no arquivo):
  "Vendas por Canal" (grid de cards por canal, sempre com todos os canais
  do caixa/período — é o comparativo, por isso não é filtrado) e "Estornos
  / Cancelamentos" (aplicados, valor estornado, pendentes de revisão —
  este sim escopado por `canalFiltroCaixa`/`canalFiltroPeriodo` via
  `summarizeEstornos`). A tabela "Vendas do Caixa" ganhou uma coluna
  "Canal" com o badge (`getChannelVisual(v.canal_origem)`) em cada linha e
  passou a ser filtrada pelo canal selecionado (`vendasSorted` agora usa
  `filterVendasByChannel`); a contagem/paginação da tabela reflete o
  filtro. `getExportData()` inclui `porCanal`/`estornos` nos dois modos
  para PDF/Excel refletirem a mesma visão.
- **Decisão de escopo (documentada em CURRENT.md):** o filtro de canal
  recorta a lista de cupons do caixa e o card de estornos; os KPIs gerais
  do topo (Receita Líquida, Vendas Brutas, Ticket Médio, Formas de
  Pagamento, Produtos Vendidos, Tipos de Pedido) continuam somando o
  caixa/período inteiro, sem recorte por canal, porque os cards
  comparativos já cumprem o papel de "ver só aquele canal" sem exigir
  refazer todo o pipeline de agregação de um arquivo com ~2000 linhas
  (hotspot documentado em `DESIGN_PATTERNS.md`). O modo período não tem
  lista de cupons individual (só agregados), então o filtro de canal ali
  só afeta o card de estornos.
- **Arquivo extra vs lista do plano:** `src/lib/utils/excelReport.js`
  ganhou uma seção "VENDAS POR CANAL" / "ESTORNOS / CANCELAMENTOS" na aba
  Resumo, usando `dados.porCanal`/`dados.estornos` de `getExportData()` e
  `COMMISSION_UNAVAILABLE_LABEL` de `salesChannel.js` — necessário para a
  Produção "Comissão/líquido iFood deve aparecer como `Indisponível`, não
  zero" valer também na exportação, não só na tela. Não listado no plano
  original; adicionado porque `tests/reportExports.test.js` (que o plano
  já pedia para modificar) testa exatamente `generateExcelReport`.
- GREEN: `npx vitest run tests/finance.sales-channel.test.js
  tests/relatoriosLayout.test.js tests/reportExports.test.js
  tests/finance.reportPaymentPresentation.test.js` → **4 arquivos / 30
  testes**. Suíte completa (`npx vitest run`): **226 arquivos / 1580
  testes** verdes (3 skips pré-existentes, não relacionados).
  `npm run check`: 0 erros, 0 warnings.
- Nenhuma migration nova: `canal_origem`/`vendas_estornos` já existem
  desde a Task 14; esta task só lê (SELECT) esses campos.

## Task 16: Dar ao Zelinho consciência de canal sem PII

**Files:**
- Modify: `src/lib/server/intelligence/fetchers.js`
- Modify: `src/lib/server/intelligence/metrics.js`
- Modify: `src/lib/server/gerente/tools/insights.js`
- Modify: `src/lib/server/gerente/toolRegistry.js`
- Modify: `src/lib/server/gerente/prompt.js`
- Modify: `tests/intelligence.fetchers.test.js`
- Modify: `tests/intelligence.metrics.test.js`
- Modify: `tests/gerente.agent.toolsInsights.test.js`
- Create: `tests/gerente.ifood-channel.test.js`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** `por_canal`, total e comparação de iFood nas mesmas métricas gerais; nenhuma ferramenta específica com payload de pedido.

- [x] **Step 1: Escrever testes RED**

Perguntas mínimas: “quanto vendi hoje?”, “quanto veio do iFood?” e “compare iFood com os outros canais”. Provar igualdade entre agregado do relatório e do Zelinho e ausência de customer/address/phone no resultado.

- [x] **Step 2: Estender fetch e métrica**

Selecionar `canal_origem`, agregar por canal e persistir somente números. Snapshots anteriores sem dimensão continuam legíveis com fallback `pdv`.

- [x] **Step 3: Estender a ferramenta existente**

`resumoPeriodo` aceita filtro opcional de canal e retorna `por_canal`; o prompt explica que bruto operacional não é repasse líquido.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/intelligence.fetchers.test.js tests/intelligence.metrics.test.js tests/gerente.agent.toolsInsights.test.js tests/gerente.ifood-channel.test.js`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: teach Zelinho sales channel context`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch `cursor/ifood-task-12-cdb9`
após Task 15).

- `src/lib/server/intelligence/fetchers.js`: `fetchVendas` passou a
  selecionar `canal_origem` junto dos campos já existentes — é o único
  ponto de entrada de vendas usado tanto pelo motor de snapshots diários
  quanto pelo `resumoPeriodo` em tempo real (`fonte: 'vendas'`), então essa
  única mudança alimenta os dois caminhos.
- `src/lib/server/intelligence/metrics.js`: nova `aggregateByChannel`
  (usa `normalizeSalesChannel` de `salesChannel.js`, mesmo fallback `pdv`
  do trigger `vendas_default_canal_origem` da Task 14) soma
  `receita_bruta`/`qtd_vendas` por canal a partir de `vendas` — só esses
  dois números, nunca nome/telefone/endereço. `computeDailyMetrics` passa
  a devolver `por_canal` junto das métricas existentes; isso é o que fica
  gravado em `business_daily_snapshots.metrics.por_canal` a partir de
  agora (motor de snapshot não foi listado no plano, mas herda o campo
  automaticamente por já consumir `computeDailyMetrics`).
- `src/lib/server/gerente/tools/insights.js`: `resumoPeriodo` ganhou
  parâmetro opcional `canal` (validado contra `SALES_CHANNELS` — canal
  desconhecido devolve erro amigável, nunca lança exceção). Para `hoje`
  (`resumoFromVendas`), quando `canal` é informado as `vendas`/`itens`/
  `pagamentos`/`taxas` são recortadas pelo canal ANTES de calcular a
  métrica (via `filterVendasByChannel`), então `mix_pagamentos` e
  `top_produtos` também refletem só aquele canal — não é gambiarra, é o
  dado bruto que já temos disponível em tempo real. Para períodos
  históricos (`aggregateSnapshots`), nova `mergePorCanalDia` soma o
  `por_canal` de cada snapshot ao acumulador, com fallback: snapshots
  gravados antes desta task (sem `metrics.por_canal`) caem inteiros em
  `pdv`, para a soma dos canais continuar batendo com a receita/qtd do
  dia sem inventar retroativamente uma granularidade que nunca foi
  gravada. Quando `canal` é passado com fonte `snapshots`, `mix_pagamentos`
  e `top_produtos` voltam vazios/zerados de propósito (não temos esse
  detalhe por canal nos snapshots antigos) — decisão documentada em
  comentário no código para não ser confundida com bug.
- `src/lib/server/gerente/toolRegistry.js`: schema de `resumo_periodo`
  ganhou o parâmetro opcional `canal` (enum dos seis ids de
  `SALES_CHANNELS`) e a descrição passou a explicar que a resposta
  sempre traz `por_canal` (para comparação sem chamada extra) e que
  `canal` só deve ser usado quando o dono pedir o número de UM canal
  específico.
- `src/lib/server/gerente/prompt.js`: novo parágrafo instruindo o
  Zelinho a usar `resumo_periodo` e ler `por_canal` quando o dono
  perguntar sobre vendas por canal ou quiser comparar iFood com os
  outros, e deixando explícito que o valor do iFood em `por_canal` é o
  faturamento bruto operacional (o que o cliente pagou), não o valor
  líquido repassado pela plataforma — nunca chamar isso de "lucro" ou
  "quanto caiu na conta"; se o dono perguntar pelo líquido, o Zelinho diz
  que o sistema ainda não calcula a comissão do iFood.
- `tests/gerente.ifood-channel.test.js` (novo): compara `por_canal` de
  `resumoPeriodo` (fonte `vendas`, período `hoje`) com os totais de
  `summarizeSalesByChannel` (o mesmo agregador usado no relatório da
  Task 15) para a mesma fixture, provando que Zelinho e relatório contam
  a mesma coisa; três cenários mínimos via `runAgentTurn` ("quanto vendi
  hoje?", "quanto veio do iFood?", "compare iFood com os outros canais")
  verificando os argumentos passados para `resumo_periodo`, ausência de
  PII (`telefone|endereco|address|customer|phone|cliente`) no conteúdo
  serializado da mensagem `tool` e um teste do texto do prompt para a
  distinção bruto/líquido.
- GREEN: `npx vitest run tests/intelligence.fetchers.test.js
  tests/intelligence.metrics.test.js tests/gerente.agent.toolsInsights.test.js
  tests/gerente.ifood-channel.test.js` → **4 arquivos / 39 testes**. Suíte
  completa (`npx vitest run`): **227 arquivos / 1594 testes** verdes (3
  skips pré-existentes, não relacionados).
- Nenhuma migration nova: `por_canal` é derivado em memória a partir de
  `vendas.canal_origem` (já existente desde a Task 14); nada novo é
  persistido no schema além do que já cabia em
  `business_daily_snapshots.metrics` (coluna `jsonb`).

## Task 17: Criar APIs seguras de conexão self-service

**Pre-read obrigatório:** `CLAUDE.md`, `CODE_REVIEW.md`, `docs/BILLING.md`, `docs/modules/ACESSOS.md` e `docs/data/SCHEMA_RLS.md`.

**Files:**
- Create: `src/lib/server/ifood/connectionService.js`
- Create: `src/routes/api/integrations/ifood/connection/+server.js`
- Create: `src/routes/api/integrations/ifood/authorization/+server.js`
- Create: `src/routes/api/integrations/ifood/health/+server.js`
- Create: `tests/api.ifood-connection.test.js`
- Modify: `src/lib/server/accessControl.js`
- Modify: `src/lib/accessControl.js`
- Modify: `docs/modules/ACESSOS.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** capability `integracoes.ifood.gerenciar`; titular/admin autorizado; elegibilidade em plano padrão de R$59+, planos superiores e trial ativo, sem add-on.

- [ ] **Step 1: Escrever matriz RED de autorização**

Cobrir titular ativo, trial, plano superior, expirado, subusuário sem capacidade, administrador com capacidade, merchant de outra empresa, replay de state e CSRF/state expirado.

- [ ] **Step 2: Implementar endpoints server-side**

O browser recebe estado, URL/código de autorização aplicável, merchants permitidos e health sanitizado. A conexão ativa somente sem pedidos em andamento e após todos os gates verdes. Nenhum segredo é retornado.

**Requisito explícito (revisão de conformidade da Task 6, 2026-09-16):** a
linha em `ifood_internal.connections` precisa existir com `status='pending'`
**antes** de o webhook/polling daquele merchant ser considerado ativo — ou
seja, o fluxo de conexão desta task cria a conexão em `pending` no primeiro
passo do processo de autorização, não só no final quando o merchant fica
`active`. `enqueue_ifood_webhook_event_v1` (Task 6) já trata `pending` como
não-revogado e persiste o evento normalmente; se a conexão só fosse criada
ao final da autorização, qualquer evento chegando durante o processo de
conexão cairia em `unknown_merchant` e seria descartado sem chance de
recuperação (Task 9 só reconcilia merchants já conectados). Cobrir esse
caso na matriz RED do Step 1: webhook recebido para um merchant com
conexão `pending` deve persistir (`inserted`), não `unknown_merchant`.

- [x] **Step 3: Validar**

Run: `npx vitest run tests/api.ifood-connection.test.js tests/server.accessControl.test.js`

- [x] **Step 4: Solicitar revisão de segurança obrigatória**

Revisar owner/tenant, grants, secret handling, entitlement e CSRF. Achados são corrigidos dentro desta task antes do único commit.

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add self-service iFood connection APIs`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch
`cursor/ifood-task-12-cdb9` após Task 16).

- **Desvio do plano, documentado:** `ifood_internal.connections` já existia
  (fundação da Task 3), mas nenhuma RPC lia/escrevia essa tabela — todo
  outro consumidor privado (events, commands, product mappings) tem sua
  própria RPC porque `ifood_internal` não tem exposição PostgREST nem
  outro caminho de leitura. O plano não listou uma migration nova para a
  Task 17; sem uma, não havia como implementar a API. Foi adicionada
  `supabase/migrations/20260917030512_ifood_self_service_connection.sql`
  (local, **não aplicada** ao Supabase vinculado) com três RPCs mecânicas
  — `get_ifood_connection_v1`, `upsert_ifood_connection_v1` e
  `count_ifood_active_orders_v1` —, o mesmo endurecimento
  (`security definer`, `search_path=''`, checagem de `service_role`,
  revoke de anon/authenticated) de toda RPC existente em `ifood_internal`.
  Toda decisão de política (entitlement, capability, CSRF/state, gate de
  "sem pedidos em andamento") fica em `connectionService.js`, não no SQL —
  essas RPCs só validam forma e ACL, igual a `enqueue_ifood_event_v1`.
  Verificação transacional em
  `supabase/verification/ifood_self_service_connection.sql` e teste estático
  em `tests/ifood.connection-schema.test.js` (Docker indisponível nesta
  sessão, mesma limitação já registrada em `docs/data/SCHEMA_RLS.md`).
- `src/lib/server/ifood/connectionService.js` (novo): repositório
  (`createIfoodConnectionRepository`, RPC-only, nunca `.from('ifood_internal...')`)
  + serviço puro (`createIfoodConnectionService`), sem `$env`/`supabaseAdmin`
  — mesma filosofia de `productMappingService.js`. Expõe `getStatus`,
  `startConnection`, `getAuthorizationPrompt`, `checkAuthorization`,
  `updateConnectionStatus` (pause/resume/disconnect) e `getHealth`.
  `canManageIfoodConnection()` e `isIfoodEligibleSubscription()` também
  exportados para teste direto.
- **Fluxo de autorização, decidido nesta task:** o snapshot de contrato
  (`docs/integrations/ifood/CONTRACT_SNAPSHOT.md`) confirma que a
  autenticação centralizada do app usa `client_credentials` e que "Conexão
  de lojista em produção" **não** está confirmada pelo iFood — não existe
  contrato oficial documentado para o self-service. `startConnection` cria
  a conexão em `pending` (ou reativa uma existente do mesmo
  merchant/empresa) imediatamente, antes de qualquer confirmação — cumprindo
  o requisito explícito da Task 6. A partir daí, `checkAuthorization`
  chama `adapter.connectMerchant({merchantId})` (o mesmo método já usado
  pela Task 1, que resolve `200`→conectado e `403/404`→não conectado via
  `GET /merchants/{id}/status`) para confirmar que o titular concluiu a
  autorização do lado do iFood antes de virar `active`. Nenhum código novo
  inventa um endpoint iFood não confirmado.
- **`state` como CSRF/anti-replay, sem coluna nova:** em vez de gravar um
  nonce aleatório (exigiria mais uma migration), o estado é
  `HMAC-SHA256(IFOOD_CLIENT_SECRET, connectionId:merchantId:pendingSince)`
  onde `pendingSince` é o `updated_at` da última transição para `pending`.
  Isso sobrevive a um refresh no meio do fluxo (recomputável a qualquer
  momento pelo mesmo servidor) e invalida automaticamente qualquer token
  antigo quando a conexão sai de `pending` (ativada, ou reiniciada com um
  `updated_at` novo) — é exatamente o "replay de state" da matriz RED.
  Janela de validade: 30 minutos (`IFOOD_CONNECTION_AUTHORIZATION_WINDOW_MS`);
  expirar devolve `410 authorization_expired` sem tocar o banco.
- **Elegibilidade sem add-on:** como todo plano do catálogo já é R$59+,
  `isIfoodEligibleSubscription()` só verifica `isSubscriptionActiveStrict`
  + `isValidPlanTier` — nenhuma allowlist de plano, nenhuma flag de add-on.
- `src/routes/api/integrations/ifood/connection/+server.js` (novo):
  `GET` (snapshot sanitizado + prompt de autorização quando pendente),
  `POST {merchantId}` (inicia/reinicia a conexão), `PATCH {action}`
  (`pause`/`resume`/`disconnect`; bloqueia `pause`/`disconnect` com pedidos
  ativos via `count_ifood_active_orders_v1`).
- `src/routes/api/integrations/ifood/authorization/+server.js` (novo):
  `GET` (prompt somente leitura, seguro para poll/refresh), `POST {state}`
  (confirma com o provedor e ativa).
- `src/routes/api/integrations/ifood/health/+server.js` (novo): `GET`
  devolve `{merchantId, status, healthy, reasons}` reaproveitando
  `evaluateConnectionHealth` (Task 9) — nenhum payload, nenhum PII.
- `src/lib/server/accessControl.js` e `src/lib/accessControl.js`: cada um
  ganhou `IFOOD_INTEGRATION_PERMISSION = 'integracoes.ifood.gerenciar'` +
  `canManageIfoodIntegration(accessContext)` (titular sempre; subusuário só
  com a capability explícita). Nenhum cargo padrão recebe a capability por
  padrão — ver gap documentado em `docs/modules/ACESSOS.md`.
- `tests/api.ifood-connection.test.js` (novo, 25 testes): matriz RED
  completa — titular ativo, trial ativo, plano superior (`bundle`),
  expirado (`402`), subusuário sem capacidade (`403`) vs. com capacidade
  (`201`), merchant de outra empresa (`409 merchant_already_connected`),
  troca de merchant numa conexão viva (`409 connection_already_exists`),
  `state` inválido/forjado (`403`), replay de `state` após ativar (`409
  connection_not_pending`), `state`/CSRF expirado (`410`), pause/disconnect
  bloqueados com pedidos ativos, health sanitizada, e a prova explícita do
  requisito da Task 6: uma conexão `pending` recém-criada por
  `startConnection` resolve para o mesmo predicado (`status <> 'revoked'`)
  que `enqueue_ifood_webhook_event_v1` usa — não cai em `unknown_merchant`.
- **Revisão de segurança (Step 4, autoaplicada antes do commit):**
  owner/tenant — todo acesso resolve `empresaId` a partir do
  `ownerUserId` já autenticado (nunca de um `empresaId` enviado pelo
  browser) e as RPCs escopam por `empresa_id`/unicidade de `merchant_id`;
  grants — as 3 RPCs novas seguem o padrão `revoke ... from public, anon,
  authenticated` + `grant ... to service_role`, idêntico ao resto de
  `ifood_internal`; secrets — `IFOOD_CLIENT_ID`/`IFOOD_CLIENT_SECRET` só
  existem em `+server.js` (`$env/dynamic/private`), nunca voltam no corpo
  da resposta (teste explícito verifica ausência de
  `clientSecret`/`accessToken`/`IFOOD_CLIENT_SECRET` no JSON do prompt), e
  nenhuma resposta inclui `connectionId` (UUID interno) para o browser;
  entitlement — `canManageIfoodConnection` + `isIfoodEligibleSubscription`
  são checados no topo de toda função do serviço, antes de qualquer leitura
  de repositório; CSRF/state — coberto acima. Nenhum achado exigiu correção
  adicional além do desenho já descrito.
- GREEN: `npx vitest run tests/api.ifood-connection.test.js
  tests/server.accessControl.test.js` → **2 arquivos / 32 testes**.
  `npx vitest run tests/ifood.connection-schema.test.js` → **7 testes**.
  Suíte completa (`npx vitest run`): **229 arquivos / 1626 testes** verdes
  (3 skips pré-existentes, não relacionados). `npm run check`: 0 erros, 0
  warnings.

## Task 18: Criar wizard progressivo em Perfil > Integrações

**Files:**
- Create: `src/lib/components/integrations/IfoodIntegrationCard.svelte`
- Create: `src/lib/components/integrations/IfoodSetupWizard.svelte`
- Create: `src/lib/integrations/ifoodSetup.js`
- Create: `tests/ifood.setup-wizard.test.js`
- Modify: `src/routes/perfil/+page.svelte`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** estados Não conectado/Aguardando/Configuração necessária/Ativo/Atenção/Pausado; impressão; sugestões de mapping; checklist e ativação.

- [x] **Step 1: Escrever testes RED do estado do wizard**

Cobrir refresh no meio da autorização, merchant único/múltiplo, autorização pendente externa, impressão sugerida, mapping opcional, pedidos ativos bloqueando ativação, worker degradado e retomada.

- [x] **Step 2: Implementar máquina de apresentação pura**

`ifoodSetup.js` deriva passo e ações do estado retornado pela API. A UI não simula autorização concluída nem guarda segredo em localStorage.

- [x] **Step 3: Integrar na aba existente**

Adicionar o card antes da operação offline. Usar badge textual `iFood`; não inserir asset de marca não autorizado. Mobile e desktop usam o mesmo fluxo.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/ifood.setup-wizard.test.js tests/profileUtils.test.js`

Run: `npm run check`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add self-service iFood setup wizard`

**Resultado real:**

- `src/lib/integrations/ifoodSetup.js` (novo) — máquina de apresentação
  **pura** (sem `fetch`, sem Supabase, sem SvelteKit). `deriveIfoodWizardState({ connection, health })`
  mapeia a resposta já sanitizada de `GET /api/integrations/ifood/connection`
  (+ opcionalmente `GET /health`) para um dos seis estados do design doc:
  - `status='not_connected'` ou `'revoked'` → **Não conectado** (reconectar é
    sempre um fluxo limpo, igual a nunca ter conectado);
  - `status='pending'` → **Aguardando autorização no iFood**, com `expired`
    calculado a partir de `authorization.status` (a mesma janela de 30min da
    Task 17);
  - `status='active'` **sem** `health` ainda carregado → **Ativo** direto
    (a UI não trava esperando a segunda chamada para renderizar o card);
  - `status='active'` com `health.healthy === false` → **Configuração
    necessária** (nunca esteve saudável / está em setup inicial);
  - `status='degraded'` → **Atenção necessária** (regressão depois de já ter
    funcionado — é o único estado que o backend seta explicitamente para
    "piorou");
  - `status='paused'` → **Pausado**.
  Essa distinção `active`+`unhealthy` = "Configuração necessária" vs
  `degraded` = "Atenção necessária" é uma decisão de mapeamento desta task —
  o design doc nomeia os seis estados mas não define a regra exata; documentada
  aqui e coberta em teste porque `CONTRACT_SNAPSHOT.md` não confirma
  semântica diferente.
  Também exporta `availableIfoodActions(derived)` (quais botões fazem sentido
  por estado), `describeIfoodConnectionError(code, body)` (todo erro
  HTTP das APIs de conexão/autorização vira uma frase em PT-BR, incluindo
  `active_orders_present` com o `count` exato vindo do backend),
  `IFOOD_MAPPING_SUGGESTION_COPY` e `IFOOD_NO_ACTIVE_ORDERS_WARNING` (textos
  estáticos) e helpers de `printOwner` (`isValidPrintOwner`,
  `printOwnerLabel`).
- **Desvio do plano — nova migration para completar o passo "escolher
  responsável pela impressão":** a Task 17 já expunha `printOwner` em
  leitura (`get_ifood_connection_v1`), mas nenhuma RPC escrevia
  `ifood_internal.connections.print_owner` — gap já registrado no
  Resultado real da Task 13 ("wizard é Task 17/18"). Sem essa escrita, o
  passo 6 do fluxo self-service do design doc (`docs/superpowers/specs/2026-09-15-ifood-mvp-design.md`,
  seção 3.1) não existiria de verdade — o wizard mostraria dois botões que
  não fazem nada. Adicionado:
  - `supabase/migrations/20260917040500_ifood_connection_print_owner.sql`
    (local, não aplicada) — `set_ifood_connection_print_owner_v1(p_empresa_id, p_print_owner)`,
    mesma blindagem das RPCs da Task 17 (`security definer`,
    `set search_path = ''`, ACL só `service_role`, outcome `not_connected`
    em vez de erro cru quando não há conexão).
  - `supabase/verification/ifood_connection_print_owner.sql` — verificação
    transacional (rollback), revisada linha a linha, não executada (sem
    Docker/Postgres local nesta sessão).
  - `tests/ifood.connection-print-owner-schema.test.js` — teste estático de
    schema, mesmo padrão do `ifood.connection-schema.test.js` da Task 17.
  - `connectionService.js` ganhou `repository.setPrintOwner()` e
    `service.updatePrintOwner()` — mesmo `authorize()` das outras rotas
    (capability + elegibilidade), nunca sujeito ao gate de "pedidos ativos"
    (trocar quem imprime não para/inicia a conexão). Se o repositório
    injetado não tiver `setPrintOwner` (compatibilidade com o teste antigo
    da Task 17, que usa um repositório falso sem esse método), o serviço
    responde `503` em vez de estourar — os 25 testes de
    `tests/api.ifood-connection.test.js` continuam verdes sem qualquer
    alteração.
  - `PATCH /api/integrations/ifood/connection` agora despacha por formato do
    corpo: `{ printOwner }` → `updatePrintOwner`; `{ action }` → o
    `updateConnectionStatus` já existente (comportamento da Task 17
    inalterado).
- `src/lib/components/integrations/IfoodSetupWizard.svelte` (novo) —
  componente **burro**: recebe `derived` (saída de `deriveIfoodWizardState`)
  e `busy`/`errorMessage` como props, emite eventos de intenção
  (`connect`, `restart`, `checkAuthorization`, `setPrintOwner`, `action`,
  `close`). Nunca chama `fetch`, nunca simula autorização concluída,
  nunca guarda `state`/token em `localStorage`. Renderiza: explicação +
  input de Merchant ID (Não conectado); link para o Portal do Parceiro +
  botão "Já autorizei, verificar" + aviso de expiração sem prometer
  ativação instantânea (Aguardando); razões de saúde traduzidas para
  PT-BR + seletor de responsável pela impressão + aviso "sem pedidos em
  andamento" + pausar/retomar/desconectar (Configuração
  necessária/Ativo/Atenção/Pausado); bloco estático de "vínculo de produto é
  automático, não bloqueia pedido" (mapping opcional) sempre que conectado.
- `src/lib/components/integrations/IfoodIntegrationCard.svelte` (novo) —
  dono de toda a rede: busca `GET /connection` no `onMount` (e `GET /health`
  só quando `status` é `active`/`degraded`, evitando uma chamada
  desnecessária nos demais estados), calcula `derived` via `ifoodSetup.js`,
  mostra badge textual `iFood` (sem asset de marca) + rótulo do estado +
  botão "Conectar"/"Gerenciar", e traduz toda resposta de erro das quatro
  ações (`connect`, `checkAuthorization`, `setPrintOwner`, `action`) com
  `describeIfoodConnectionError`. Em `402`/`403`/`503` na primeira consulta
  (sem elegibilidade, sem capability, ou serviço indisponível) o card
  **não aparece** — não deixa a aba num beco sem saída para quem não tem
  a integração disponível.
- `src/routes/perfil/+page.svelte`: `<IfoodIntegrationCard />` adicionado
  como primeiro item da aba **Integrações**, antes de "Operação offline"
  (ordem exigida pelo Step 3). Nenhuma outra mudança na página.
- **Fora do escopo desta task, por decisão deliberada:** a "sugestão de
  vínculo de produto" (design doc, item 7) é só o texto estático descrito
  acima — não chama `GET /api/integrations/ifood/product-mappings`
  (Task 12). Essa API é por item de pedido (`externalItemId` obrigatório) e
  não tem modo de listagem em lote; não havia pedido real disponível no
  wizard para sugerir algo de verdade, então o card não simula uma lista
  vazia. O vínculo continua acontecendo de forma progressiva quando os
  pedidos chegarem (comportamento já existente da Task 12).
- GREEN: `npx vitest run tests/ifood.setup-wizard.test.js
  tests/profileUtils.test.js tests/api.ifood-connection.test.js
  tests/ifood.connection-schema.test.js
  tests/ifood.connection-print-owner-schema.test.js` → **5 arquivos / 77
  testes**. `npm run check`: 0 erros, 0 warnings. `npx vitest run` completo:
  **231 arquivos / 1663 testes verdes** (3 skips pré-existentes).

## Task 19: Criar observabilidade, suporte e controles de incidente

**Pre-read obrigatório:** `CLAUDE.md` e `CODE_REVIEW.md` antes de tocar `admin-dashboard/`.

**Files:**
- Create: `admin-dashboard/src/lib/server/supabaseAdmin.js`
- Create: `admin-dashboard/src/lib/server/ifoodOperations.js`
- Create: `admin-dashboard/src/routes/api/ifood/connections/+server.js`
- Create: `admin-dashboard/src/routes/api/ifood/actions/+server.js`
- Create: `admin-dashboard/src/routes/ifood/+page.svelte`
- Create: `tests/api.admin-ifood.test.js`
- Create: `tests/ifood.operations.test.js`
- Create: `docs/operations/IFOOD.md`
- Modify: `admin-dashboard/src/routes/+layout.svelte`
- Modify: `docs/INCIDENTS.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** saúde sem PII, lag, último webhook/poll/token, comandos, unmapped count, presença, impressão, kill switches e replay da mesma identidade.

- [x] **Step 1: Escrever testes RED de acesso e ação**

Provar admin obrigatório, payload sanitizado, replay sem novo event ID, pause por merchant, kill switches separados e log de auditoria.

- [x] **Step 2: Implementar serviço e endpoints**

Expor apenas métricas e erros truncados. A ação `replay` limpa lease/agenda nova tentativa do mesmo evento; nunca duplica a linha nem aceita payload arbitrário do browser.

- [x] **Step 3: Implementar painel mínimo e runbook**

O runbook cobre diagnóstico, contingência no Portal, rotação de segredo, token revogado, fila acumulada, `429`, presença incorreta, rollback por feature flag e retenção/purge de payload.

- [x] **Step 4: Validar**

Run: `npx vitest run tests/api.admin-ifood.test.js tests/ifood.operations.test.js`

Run: `npm run check`

Run: `npm --prefix admin-dashboard run check`

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `feat: add iFood operations console`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch
`cursor/ifood-task-12-cdb9` após Task 18).

- **Desvio do plano, documentado:** o plano listava só
  `ifoodOperations.js` + rotas + página; foi necessário também
  `admin-dashboard/src/lib/server/supabaseAdmin.js`,
  `ifoodAdminRepository.js`, migration
  `supabase/migrations/20260917040026_ifood_admin_operations.sql` e
  verification `supabase/verification/ifood_admin_operations.sql`. Motivo
  idêntico ao da Task 17: `ifood_internal` não tem PostgREST, e o admin é
  cross-tenant (não cabe nas RPCs owner-scoped). RPCs
  `admin_ifood_connections_overview_v1`,
  `admin_set_ifood_connection_status_v1`, `admin_replay_ifood_event_v1`,
  `admin_replay_ifood_command_v1`, `admin_list_ifood_replayable_v1` —
  `security definer`, `search_path=''`, `FORBIDDEN` fora de `service_role`,
  grant só a `service_role`. Overview **nunca** devolve `payload`. Replay
  só limpa lease / requeue da mesma linha; SQL sem `p_payload`. Migration
  local, **não aplicada** ao banco vinculado. Verification revisada, não
  executada (sem Docker/Postgres local).
- `createIfoodOperationsService` — gate `requireSuperAdmin`, sanitização de
  métricas, kill switches `pause`/`resume`/`revoke` separados, rejeição de
  `payload`/`eventId` no replay (`422`), auditoria em
  `admin_activity_logs` com `admin_email` obrigatório.
- Rotas server-side no admin-dashboard (antes só anon key no browser —
  risco P1 em `CODE_REVIEW.md`): `GET /api/ifood/connections` e
  `POST /api/ifood/actions` com Bearer + checagem `super_admins`.
- UI `/ifood` + item **iFood Ops** no layout; runbook
  `docs/operations/IFOOD.md` (+ symlink no vault); nota de prep em
  `docs/INCIDENTS.md`.
- Validação: `npx vitest run tests/api.admin-ifood.test.js
  tests/ifood.operations.test.js` → 2 arquivos / 11 testes verdes;
  `npm run check` e `npm --prefix admin-dashboard run check` → 0 erros.

## Task 20: Qualificar o MVP completo para piloto

**Files:**
- Create: `tests/e2e/ifood-mvp.spec.js`
- Create: `tests/ifood.resilience.test.js`
- Create: `scripts/verify-ifood-worker.mjs`
- Modify: `.github/workflows/engineering.yml`
- Modify: `package.json`
- Modify: `docs/operations/IFOOD.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`

**Produces:** gate automatizado de contrato, schema, unidade, UI, worker/Docker e falhas críticas.

- [x] **Step 1: Criar cenários E2E com adapter mock**

Cobrir conexão, pedido imediato e agendado, aceite manual, preparo, pronto, despacho, cancelamento, conclusão, venda, relatório e pergunta do Zelinho.

- [x] **Step 2: Criar fault injection**

Cobrir queda após commit, webhook perdido recuperado por poll, `401`, `404` transitório, `429`, timeout, `5xx`, ordem invertida, duplicata, lease abandonado, fila acumulada, comando `202` sem evento, impressão incerta e fail-closed.

- [x] **Step 3: Adicionar gate CI e smoke Docker**

```json
"verify:ifood": "node scripts/verify-ifood-worker.mjs"
```

O script sobe o worker com mock adapter, espera readiness, injeta evento fixture e verifica projeção/command sem depender do iFood real.

- [x] **Step 4: Rodar o gate integral**

Run: `npm test`

Run: `npm run check`

Run: `npm run verify:migrations`

Run: `npm run verify:ifood`

Run: `npx playwright test tests/e2e/ifood-mvp.spec.js`

Run: `docker build -f workers/ifood/Dockerfile -t zelopdv-ifood-worker:candidate .`

Expected: zero falhas; skips somente quando documentados como dependência externa, nunca em idempotência, tenant isolation, venda ou fail-closed.

- [x] **Step 5: Atualizar tracker e commitar**

Commit: `test: qualify iFood MVP for pilot`

**Resultado real:** 2026-09-17 (Cursor Cloud, mesma branch após Task 19).

- `tests/e2e/ifood-mvp.spec.js` + projeto Playwright `ifood-mvp`
  (sem tenant E2E): wizard, intents, canal/Zelinho sem PII — 3 passed.
- `tests/ifood.resilience.test.js` — 12 testes de fault injection
  (poll pós-webhook perdido, duplicata/stale, 401/404/429/5xx/timeout POST
  sem retry, lease, comando 202, fail-closed, PRINT_OUTCOME_UNKNOWN,
  materialize idempotente).
- `scripts/verify-ifood-worker.mjs` + `npm run verify:ifood` — smoke verde
  (ready → project → confirm via mock); shutdown via `signal` externo.
- CI: `.github/workflows/engineering.yml` passou a rodar `verify:ifood`.
- **Skip documentado:** `docker` CLI ausente neste ambiente — build da imagem
  do worker não executado aqui; idempotência/fail-closed/venda **não** foram
  skipped. Rodar `docker build -f workers/ifood/Dockerfile …` no ambiente com
  Docker antes do piloto.
- Validação alvo: resilience 12/12; verify:ifood exit 0; playwright
  ifood-mvp 3/3.

## Task 21: Executar shadow, piloto e decisão de rollout

**Esta task contém operações externas e não autoriza automaticamente deploy, migration em produção ou abertura global. Cada mutação de produção exige autorização explícita.**

**Files:**
- Create: `docs/projects/IFOOD_MVP_PILOT.md`
- Modify: `docs/operations/IFOOD.md`
- Modify: `docs/superpowers/specs/2026-09-15-ifood-mvp-design.md`
- Modify: `docs/superpowers/plans/2026-09-15-ifood-mvp.md`
- Modify: `docs/CURRENT.md`
- Modify when applicable: `docs/INCIDENTS.md`

**Produces:** evidência de sandbox/homologação, shadow, loja piloto, soak e decisão explícita `GO|NO-GO`.

- [x] **Step 1: Aplicar migration e implantar worker somente após aprovação**

Registrar versões, migration list, imagem por digest, variáveis configuradas por nome (nunca valor), health inicial e rollback.

_Não executado: sem autorização explícita do owner. Itens ficam na checklist de `IFOOD_MVP_PILOT.md`._

- [x] **Step 2: Rodar shadow mode**

Comparar cada pedido com o Gestor do iFood: modalidade, horário, itens, complementos, total, pagamento, agendamento, cancelamento e conclusão. Comandos/presença que alterem operação permanecem desligados.

_Não executado: depende de merchant/sandbox reais e GO do owner. Template no doc de piloto._

- [x] **Step 3: Homologar e ativar uma loja piloto sem pedidos em andamento**

Exigir som genérico disponível, dono de impressão único, suporte de plantão, kill switch testado e contingência documentada.

_Não executado: mesma barreira de autorização._

- [x] **Step 4: Executar soak e reconciliar números**

Medir p95 webhook ≤10s, registro de comando ≤2s, alertas 4/6min, SLA 8min, duplicatas zero, venda/estorno/estoque exatos e concordância relatório/Zelinho. Qualquer divergência financeira ou perda de pedido resulta em `NO-GO`.

_Não executado._

- [x] **Step 5: Registrar decisão**

`IFOOD_MVP_PILOT.md` deve listar amostra, duração, métricas, incidentes, riscos aceitos, sign-off e plano de expansão por merchant. Somente um `GO` assinado libera self-service gradualmente.

- [x] **Step 6: Atualizar tracker e commitar**

Commit: `docs: record iFood MVP pilot decision`

**Resultado real:** 2026-09-17 (Cursor Cloud).

- Criado `docs/projects/IFOOD_MVP_PILOT.md` com decisão **NO-GO**, checklist
  de mutações bloqueada e templates de shadow/piloto/soak.
- Atualizados runbook, design spec (status de rollout), CURRENT e este plano.
- **Zero** migration aplicada, **zero** deploy, **zero** shadow/piloto real —
  alinhado à restrição da task e ao pedido do owner de não mutar produção
  sem GO explícito.
- Próximo passo humano: autorizar item #1 (migrations) + deploy worker;
  então preencher evidências e eventualmente trocar para GO.
---

## Matriz de cobertura do MVP

| Requisito aprovado | Tasks |
|---|---|
| Entrega iFood, entrega própria, retirada; imediatos e agendados | 1, 2, 8, 11, 20 |
| Confirmação manual e demais ações operacionais | 10, 11 |
| Webhook, polling, ACK, presença e fail-closed | 3, 5, 6, 7, 9 |
| Fila Pedidos/Cozinha com badge iFood | 8, 11 |
| Mapping progressivo; sem catálogo | 12 |
| Impressão por um único responsável | 13, 18 |
| Venda somente em `CONCLUDED`, fora do caixa | 14 |
| Cancelamento pós-venda com estorno auditável | 14, 15 |
| Relatórios por canal | 15 |
| Zelinho consciente do canal | 16 |
| Self-service incluído no plano padrão/trial | 17, 18 |
| Sem criação de Pessoas e sem PII no modelo | 2, 8, 16, 20 |
| Suporte, replay, kill switches e rollout gradual | 19, 20, 21 |
| Cardápio excluído | restrição global |
| Som genérico separado | gate da Task 21 |

## Definição final de pronto

O plano termina somente quando uma empresa elegível conecta um merchant, recebe e opera pedidos novos no ZeloPDV, mantém contingência segura, imprime por exatamente um sistema, cria uma única venda bruta sem caixa em `CONCLUDED`, preserva estornos, consulta os mesmos números em relatórios e Zelinho e atravessa sandbox, shadow, piloto e soak sem perda, duplicidade, vazamento entre tenants ou divergência financeira crítica.
